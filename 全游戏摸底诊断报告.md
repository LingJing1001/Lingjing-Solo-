# 灵境战队 AGI 课题探索 · 全游戏摸底诊断报告

> 日期：2026-08-28  
> 对象：[LingJing1001/Lingjing-Solo-](https://github.com/LingJing1001/Lingjing-Solo-)（本地目录：`灵境战队AGI课题探索`）  
> 对照基线：`ARC-AGI-3-Kaggle-Starter/agent/my_agent.py`（含 `ls20_solver`）  
> 看板：http://127.0.0.1:8781（数据文件：`ui/static/games_benchmark_team.json`）

---

## 1. 结论摘要

| 项目 | 课题探索版（GitHub） | Starter 版（本地增强） |
|------|----------------------|------------------------|
| 全游戏总分（25 局平均） | **0.00** | **≈ 0.14** |
| 总过关数 | **0** | **1**（仅 ls20 L1） |
| 包路径是否加载正确 | **正确** | 正确 |
| 是否具备专用解题器 | **无** | 有 `ls20_solver` |

**一句话：**  
加载没有搞错；零关是因为 GitHub 仓库是「五层框架骨架」，没有针对 ARC-AGI-3 各游戏的可过关策略。本地 Starter 能过 ls20 第 1 关，靠的是后来加的专用求解器，**不在**该 GitHub 仓库里。

---

## 2. 摸底结果（课题探索版）

- 环境数：25  
- 每局上限：400 步  
- 总过关数：**0**  
- 全胜局数：0  
- 总分（scorecard 平均）：**0.00**  
- 耗时：约 36 秒  

所有游戏（含键盘关 `ls20` / `wa30`、点击关 `vc33` 等）均为 `levels_completed = 0`。

计分提醒（ARC-AGI-3）：

- 单局分 ≈ 各关 \((基准步数 / 实际步数)^2 × 100\) 加权平均  
- **总分 = 全部游戏分数的平均值**  
- 因此「只啃一个游戏」涨分极慢；多游戏各过 1 关更划算  

---

## 3. 加载是否正确？（已核实）

诊断脚本：`ARC-AGI-3-Kaggle-Starter/scripts/diag_team_agent.py`  
适配入口：`ARC-AGI-3-Kaggle-Starter/agent/lingjing_team_agent.py`

| 检查项 | 结果 |
|--------|------|
| 实际 `import lingjing_solo` 路径 | `…/灵境战队AGI课题探索/lingjing_solo/` |
| 是否误用 Starter 包 | 否 |
| 是否存在 `planning/ls20_solver.py` | **否**（课题探索版应无） |
| 网格读取（RESET 后） | 正常 64×64 |
| 动作桥接 UP→ACTION1 等 | 已实现，环境可收到合法 `GameAction` |

**因此：不是「加载错包」，而是「这版策略过不了关」。**

---

## 4. 根因分析

### 4.1 架构定位：框架 ≠ 能打的 Agent

GitHub 仓库说明书强调的是：

- 五层管道：感知 → 世界模型场 Φ → 探索 → 规划 → 反思  
- LLM 降为「受限预算顾问」，无网评测可降级为纯规则  

这解决的是**工程结构 / 论文叙事**，并不自动等于「能在 25 个交互游戏里过关」。

### 4.2 决策退化为「总选第一个动作」

同局对照（ls20，前十几步）：

| 版本 | 典型动作序列 | 约 20 步过关 |
|------|--------------|--------------|
| 课题探索 | `RESET` 后连续 **ACTION1（向上）** | 0 |
| Starter | BFS 组合：左/上/右导航到旋转台与目标 | **1** |

课题探索版内部默认动作名为 `UP/DOWN/LEFT/RIGHT/SPACE`，适配层已映射到官方 `ACTION1–5`；但轻量规划 / 探索评分几乎总返回列表首项 → 表现为一直向上撞墙，无法完成旋转台 + 目标格流程。

### 4.3 缺少游戏专用知识

ARC-AGI-3 本地公开集约 25 个环境，机制差异大：

| 类型 | 数量级 | 课题探索现状 |
|------|--------|--------------|
| 纯键盘 | 少（如 ls20） | 无专用 solver |
| 纯点击 | 若干（如 vc33） | 默认关鼠标，几乎无法操作 |
| 键盘+点击混合 | 多数 | 通用探索 400 步内过不了第 1 关 |

Starter 版之所以能过 **ls20 L1**，是因为有手写的：

- 5 像素网格 BFS  
- 旋转台 / 目标视觉检测  
- 关卡切换等待  

这些逻辑在课题探索仓库中**不存在**。

### 4.4 接口与评测约束未完全对齐（次要，但需改）

| 点 | 课题探索仓库 | ARC 官方 / Starter |
|----|--------------|---------------------|
| Agent 基类 | 自包含 `LingjingSoloAgent` | 需继承 `agents.agent.Agent` |
| 动作名 | UP/DOWN/… | ACTION1–6 |
| 帧格式 | 期望 `.grid` | `FrameData.frame` 多层网格 |
| 结束条件 | 内部 `detect_win` + 步数上限 | 应以 `GameState.WIN` / `levels_completed` 为准 |

本地已用 `lingjing_team_agent.py` 做桥接；桥接保证「能跑」，不保证「能赢」。

---

## 5. 与「榜上 5 点几」的关系

| 来源 | 含义 |
|------|------|
| 本次课题探索摸底 0.00 | 纯框架、无专用策略 |
| 本地 Starter 摸底 ≈ 0.14 | 仅 ls20 过 1 关的分数摊到 25 局 |
| Kaggle 榜 5.x | 通常意味着**多个环境**已有可见过关，或使用了更强提交 |

**课题探索仓库本身不能解释 5.x 成绩**；要冲 6+，必须在多游戏上增加可过关能力，而不是只完善架构文档。

---

## 6. 改进办法（按优先级）

### P0 — 先能过关（提分最快）

1. **保留框架，接入专用求解器**  
   - 把 Starter 中已验证的 `ls20_solver` 并入课题探索版的决策优先级最前（识别到 ls20 布局则走 BFS）。  
   - 目标：恢复 ls20 ≥ 1 关，总分回到约 0.1～0.2 量级。

2. **按标签扩广度，而不是死磕 L2**  
   - 优先「键盘」关：`wa30`、`g50t`、`tr87`（与 ls20 同类）。  
   - 每个游戏先冲 **过第 1 关**；25 局里多过几关，比 ls20 多过 1 关更涨总分。

3. **点击关打开 ACTION6**  
   - `SoloConfig.enable_mouse=True`，并接入泡壁/网格点击探索（Starter 已有雏形）。  
   - 否则 `vc33` 等 click 标签游戏几乎必为 0 分。

### P1 — 让通用层真正「会选动作」

4. **修复探索 / 规划退化**  
   - 禁止长期重复同一 noop 动作；加入卡住检测与强制换向。  
   - 用帧差分估计动作效应（effect EMA），按信息增益采样，而不是 `valid[0]`。

5. **统一动作与帧适配进仓库本体**  
   - 在 `lingjing_solo` 内原生支持 `ACTION1–6` 与 `FrameData.frame`，减少外部桥接出错面。  
   - `is_done` 以环境 `WIN` / `levels_completed` 为准，弱化不可靠的视觉 `detect_win`。

### P2 — 工程与验证闭环

6. **固定摸底流水线**（已具备雏形）  
   ```powershell
   cd ARC-AGI-3-Kaggle-Starter
   .\.venv\Scripts\python.exe scripts\benchmark_all_games.py 400 `
     --agent agent/lingjing_team_agent.py `
     --out ui/static/games_benchmark_team.json `
     --label "灵境战队AGI课题探索"
   .\.venv\Scripts\python.exe scripts\games_benchmark_ui.py --port 8781
   ```  
   - 每次改策略后看：`total_levels_completed`、`aggregate_score`、`priority_fix`。

7. **对照回归**  
   - 同预算对比 Starter vs 课题探索（见 `scripts/diag_team_agent.py`）。  
   - 验收门槛建议：  
     - 短期：ls20 ≥ 1 关  
     - 中期：≥ 5 个游戏各 ≥ 1 关  
     - 冲榜：总分稳定进入 5+ / 6+ 再谈架构锦上添花  

### P3 — 文档与仓库定位

8. **在 README 写清能力边界**  
   - 「五层 Φ-Field」= 学习与规划框架；  
   - 「过关」= 另需游戏机制模块 / 专用 solver。  
   - 避免读者误以为 clone 后即可冲榜。

9. **开源与比赛合规**  
   - Kaggle 无网评测：默认 `llm_calls_per_game=0`（仓库已如此）。  
   - 获奖须开源：专用 solver 也应逐步纳入仓库并文档化。

---

## 7. 建议的近期执行顺序

```text
第 1 周
  ├─ 合并 ls20_solver → 课题探索决策链最前
  ├─ 打开鼠标点击 + 基础 click 探索
  └─ 全游戏摸底，目标：总分 > 0，过关数 ≥ 1

第 2 周
  ├─ 复制 ls20 套路到 1～2 个键盘关（wa30 / g50t）
  ├─ 修复「总选 ACTION1」卡住逻辑
  └─ 目标：过关分布到 ≥ 3 个游戏

第 3 周+
  ├─ 按看板 priority_fix 扫 0 关游戏
  └─ 再评估是否值得攻坚 ls20 L2（单关涨分有限）
```

---

## 8. 附录：关键路径

| 说明 | 路径 |
|------|------|
| 课题探索仓库（clone） | `灵境战队AGI课题探索/` |
| 桥接 Agent | `ARC-AGI-3-Kaggle-Starter/agent/lingjing_team_agent.py` |
| 摸底脚本 | `ARC-AGI-3-Kaggle-Starter/scripts/benchmark_all_games.py` |
| 诊断脚本 | `ARC-AGI-3-Kaggle-Starter/scripts/diag_team_agent.py` |
| 课题探索摸底 JSON | `ARC-AGI-3-Kaggle-Starter/ui/static/games_benchmark_team.json` |
| Starter 摸底 JSON | `ARC-AGI-3-Kaggle-Starter/ui/static/games_benchmark.json` |
| 可视化看板 | `scripts/games_benchmark_ui.py` → http://127.0.0.1:8781 |

---

## 9. 最终判断

- **加载：正确。**  
- **零关原因：策略与游戏机制不匹配 + 无专用解题器 + 决策退化为单方向动作。**  
- **改进核心：在保持 Φ-Field 框架的前提下，优先移植/扩展可过关模块（先 ls20，再多游戏广度），并用全游戏摸底看板验收，而不是继续只完善架构叙述。**
