# v10 Planner/WMP 嵌入测试报告

- **日期**：2026-08-30（更新）
- **范围**：将 `lingjing_solo_v10` 的 WMP + Planner 以可选桥接方式接入团队 agent  

## 现状

| 项 | 状态 |
|----|------|
| v10 源码包 `lingjing_solo_v10/` | 仍在课题探索仓库 |
| 单元 / MiniSokoban 测试 | 历史已通过（见下方原记录） |
| **主循环接入** | `LINGJING_USE_WMP=1` 时由 `lingjing_solo/agent_wmp_bridge.py` 提供建议；**默认关闭** |
| 决策优先级 | ScriptBank → LS20Solver → WMP(可选) → Φ-Field |

## 使用

```powershell
$env:LINGJING_USE_WMP="1"
.\.venv\Scripts\python.exe scripts\play_local.py --game vc33 --agent agent/lingjing_team_agent.py --max-steps 100
```

注意：当前桥接用色块粗符号表，**不保证涨分**；出分仍以 ScriptBank 为准。

## 原嵌入结论（保留）

1. 嵌入成功且未破坏原 `test_solo` 五层闭环。  
2. `LingjingSoloAgent` Φ 路径仍是默认；WMP 为旁路。  
3. P0 出分应优先 ScriptBank / ls20 / ar25，而非 WMP。
