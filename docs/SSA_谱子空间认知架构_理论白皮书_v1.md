# SSA：谱-子空间认知架构（Spectral–Subspace Architecture）
## 面向 ARC-AGI-3 的 AGI 进化理论白皮书 v1.0

> 日期：2026-09-06
> 数学基础：Gilbert Strang《线性代数（原书第6版）》（以下简称 [S6]）全部十章
> 落地仓库：`ARC-AGI-3-Kaggle-Starter/lingjing_solo/lincore/`（本次新增）
> 上游理论：灵境-场架构（Φ 场 / 泡壁）、CEAX v0.4（假设-实验-迁移闭环）、ScriptBank（已验证技能）
> 定位：**统一前述三者的数学内核**，并用世界最前沿研究校准方向

---

## 0. 一页摘要

智能体面对的每个 ARC-AGI-3 游戏，都是一个**未知离散算子族**：

$$x_{t+1} = T_{a_t}(x_t), \quad a_t \in \{ACTION1..7\}$$

没有任何说明书（无自然语言、无规则先验），只有 64×64×16 的观测流。SSA 的核心主张是：

> **智能 = 在未知算子下，同时最小化预测残差 r、最大化可控增益 g、对齐进度方向 s 的在线闭环过程；而这三件事的全部数学，就是线性代数的四个基本子空间、SVD、最小二乘、特征值与对偶优化。**

三个标量信号统摄一切认知行为：

| 信号 | 定义 | 数学根源 | 认知功能 |
|------|------|----------|----------|
| **r（惊讶）** | ‖(I − P_row)·Δ‖ / ‖Δ‖ | [S6] Ch3/7 正交补 | 好奇心、探索采样 |
| **g（可控）** | ‖E_a‖_F² / n_a | [S6] Ch6 谱范数 / empowerment | 价值、因果杠杆 |
| **s（进度）** | ⟨Δ, p*⟩ / (‖Δ‖‖p*‖) | [S6] Ch4 最小二乘 + Ch1 内积 | 目标对齐、利用 |

其中 Δ = φ(x_{t+1}) − φ(x_t) 是效应特征，P_row 是向已辨识行空间的正交投影，p* 是由"关卡进度"标签经岭回归学出的**进度奇异方向**。

**AGI 进化**由此形式化：跨任务地扩大三信号的联合可辨识域 —— 记忆 = 跨游戏低秩共享子空间（矩阵补全），进化 = 子空间体积（Gram 行列式）驱动的开放式课程。

---

## 1. 第一性原理：为什么是"子空间"

### 1.1 认知的三大正交问题

任何在未知环境中求生的智能体，都在隐式求解三个问题：

1. **表征问题**（"世界该用什么坐标描述？"）
   → 找基 V_k 使 φ(x) 可被低维张成。Eckart–Young 定理 [S6 §7.3] 保证 SVD 截断是 Frobenius 范数意义下的**最优**低秩逼近。这不是工程选择，是数学最优。

2. **辨识问题**（"动作如何改变世界？"）
   → 把实验-效应对堆叠成效应矩阵 E，用最小二乘 [S6 §4.3] 估计转移结构。**四大基本子空间 [S6 Ch3] 恰好给出"哪些可辨识"的完备分类**（§2.2）。

3. **控制问题**（"如何用最少动作到达目标？"）
   → 在预算约束下最大化 s。约束优化的语言是拉格朗日对偶 [S6 §9.3–9.4]，多步前瞻的语言是 Krylov 子空间 [S6 §6.x, 电力迭代]。

### 1.2 自由能原理的线性代数化

Friston 自由能原理（Active Inference）说：生物 minimize 变分自由能 F = 意外项 + 复杂度项。在线性高斯模型下这**恰好**是岭回归目标：

$$\min_\theta \;\underbrace{\tfrac{1}{2\sigma^2}\|E\theta - y\|^2}_{\text{预测残差（意外）}} + \underbrace{\tfrac{1}{2\lambda}\|\theta\|^2}_{\text{复杂度惩罚（先验）}}$$

[S6 §6.3.7"最优化和机器学习"、§4.3 最小二乘]。因此 SSA 的每个在线拟合器都是自由能最小化的一次可实现退化 —— 理论与实现零缝隙。

### 1.3 为什么不用端到端深度网络

Kaggle 评测**无互联网、无 GPU**（本项目 notebook 默认 cpu）。端到端网络（Dreamer 类）需要训练数据与算力，而 ARC-AGI-3 每局最多 800 步交互、必须**在线**学习。谱方法的优势：

- **样本复杂度低**：最小二乘是一次闭式解，无需迭代训练；
- **算力低**：64 维块特征 / 64×7 区域-动作矩阵的 SVD 是毫秒级；
- **可解释可证伪**：每个假设对应一个线性约束，可被一次实验推翻 —— 这正是 CEAX 假设闭环需要的"可验证性"（参见 `docs/FALSE_SOLUTION_CHECKLIST.md` 的假解防护哲学）；
- **数学最优性有定理背书**（Eckart–Young、最小范数广义逆、Perron 特征方向），而非经验调参。

---

## 2. 数学基础 → 认知组件：逐章映射

以 [S6] 十章为骨架，每章提炼出**可执行**的认知机制：

### Ch1 向量和矩阵 → 状态空间与规则原子化
- 状态 = 向量 x ∈ R^d（φ 编码，§5.1）；动作组合 = 线性组合 cv + dw [§1.1.1]。
- **A = CR 分解** [§1.4.4–1.4.8]：规则库的数学化 —— C 的列 = 秩-1 规则原语（`rule_primitives.py` 的形式化对应），R 的行 = 原语组合系数。ScriptBank 里每条脚本就是一个"秩-1 效应基元 + 触发上下文"。

### Ch2 解 Ax = b → 因果辨识与假设消元
- 每个假设 h 对效应数据施加线性约束 E h ≈ y；消元 [§2.1] = 排除矛盾假设；LU 可复用性 [§2.3] = 换观测不换算子的增量推断。
- 消元失效 [§1.1.3, §2.1.3] ⇔ 假设不可辨识 ⇔ 需要设计**区分性实验**（CEAX experiment 的数学判据：新实验必须增大 E 的秩）。

### Ch3 四个基本子空间 → 世界模型可辨识性定理（本架构的定理核心）
对效应矩阵 E ∈ R^{m×d}（SVD 后）：

| 子空间 | 基 | 认知含义 |
|--------|-----|----------|
| 列空间 C(E) | U 的前 r 列 | **能力边界**：已证实的效应流形 |
| 零空间 N(E) | V 的后 d−r 列 | **不变量/背景法则**：任何已试动作都不改变的坐标 |
| 行空间 C(Eᵀ) | V 的前 r 列 | **相关特征**：预测结局真正用到的维度 |
| 左零空间 N(Eᵀ) | U 的后 m−r 列 | **冗余实验**：重复信息（不奖励重复探索） |

推论（冷启动协议）：新游戏先探测 N(E) 的支撑集 —— 一次性排除 90% 静态像素，把注意力预算集中到"活"坐标。这是**可辨识性优先**的探索：不是盲目扫格子，而是先问"哪里可能出效应"。

### Ch4 正交性 → 记忆、投影与最优迁移
- 记忆检索 = 向量到子空间的投影 [§4.2]：相似状态 = 投影距离近。
- **广义逆 A⁺ = R⁺C⁺** [§4.5.3]：跨任务迁移的**最小范数解** —— 在旧游戏子空间与新游戏约束之间，取扰动最小的插值。这就是 `MatrixMemory` 低秩补全的理论依据（避免迁移"幻觉"：最小范数 = 不臆造新方向）。
- Gram–Schmidt [§4.4.2] = 技能库去重：新技能须对现有基正交补有分量才入库。

### Ch5 行列式 → 多样性与课程
- det(Gram(V)) = 特征所张平行体体积 [§5.3]：**探索多样性的严格度量**。课程生成 = 选使体积增益最大的下一任务（开放式进化，§7 E5）。
- 克莱姆法则 [§5.2.2] = 反事实唯一性判据：可逆时反事实唯一 → 可安全利用；奇异时 → 必须先做区分实验。

### Ch6 特征值与特征向量 → 动力学分层与时间尺度
- 转移算子的谱 = **时间尺度金字塔**：|λ|≈1 的方向是慢变量（对象、墙、已解锁结构），λ≈0 是快变量（噪声、抖动）。对象 = 谱意义下的"准不动子空间"。
- du/dt = Au 的稳定性 [§6.5]：谱半径 < 1 ⇒ 关卡动力学收敛（死锁检测：连续效应谱半径 → 0 且无进度 ⇒ 触发 RESET/换策略）。
- **循环矩阵与 FFT** [§6.4.6–6.4.9]：所有循环矩阵共享傅里叶特征基 ⇒ 周期机制检测 = 对变化序列做 DFT 找主峰。闪烁门、周期陷阱（ls20 类游戏常见）由此显式建模，规划可预言相位。
- Perron–Frobenius [§6.2/6.3]：非负转移矩阵的主特征方向 = 系统的"必然去向" —— 页面排名式的目标显著性。

### Ch7 SVD 与 PCA → 压缩、新奇与对象分割
- Eckart–Young [§7.1/7.3]：状态压缩的最优截断。
- **新奇 = 残差子空间距离**：n(x) = ‖(I − V_k V_kᵀ)Δ‖ —— 落在已学流形之外的程度。这是 RND/Bayesian-surprise 的确定性线性替身。
- 谱聚类 [§7.3 PCA 几何 + 图拉普拉斯]：对象分割 = 共变化矩阵的第二小特征向量切分（路线图 E4）。
- 埃克哈特-扬定理的几何意义 [§7.3.4]：椭球主轴 = 效应协方差的主方向 ⇒ 点击目标的"效应椭球"方向预测。

### Ch8 线性映射 → 技能即算子复合、跨游戏同构
- 技能 = 复合映射 TS [§8.2.3]：ScriptBank 脚本的数学身份。技能库 = 算子代数的生成集。
- **基变换 [§8.2.1] = 跨游戏同构**：新游戏状态嵌入旧游戏的谱基（共享 V_k），迁移 = 坐标变换而非重新学习。这是"灵境"跨任务记忆的严格形式。

### Ch9 最优化 → 在线学习与稳健决策
- 梯度下降 = 最速下降 [§9.1.1]，动量 = 重球路径 [§9.1.5]：假设参数的在线精调（`gradient.py`）。
- SGD + 反向传播 [§9.2]：进度预测器的流式训练；学习率 = 步长的"之字形"抑制 [§9.1.4]。
- **拉格朗日对偶 [§9.3–9.4] = 探索预算调度**：约束 Σt_explore ≤ B 的对偶变量 λ 随预算消耗上升 → 探索概率自动衰减。minimax [§9.4.4] = 动作选择的稳健化（在效应不确定性上取最坏情形不差的方向）。
- 半定规划 [§9.4.5]（路线图）：探索协方差 Σ ⪰ εI 的维持。

### Ch10 数据学习 → 与深度学习的接口
- 深层网络 = 分段线性函数 [§10.1]：LLM 顾问输出的"脚本建议"本质是分段线性策略的分段选择；谱内核负责**验证**其所在线性片是否真的有效。
- 卷积 = 权重共享 = 循环矩阵 [§10.2.2 + §6.4]：局内局部规则检测可用 FFT 加速（对 64×64 网格做核匹配代价 O(N log N)）。
- 协方差与联合分布 [§10.3]：技能相关性矩阵 V 的半正定性 [§10.3.7] ⇒ 技能库无冗余维的正定化裁剪。
- 双重下降/泛化 [§10.2.1]：迁移先验的正则强度 = 岭参数 λ 的跨游戏调度。

---

## 3. SSA 六层架构

```
        ┌─────────────────────────────────────────────────────┐
        │  L5 矩阵记忆与进化   MatrixMemory (低秩补全迁移)      │
        │     + det(Gram) 多样性课程（开放进化）                │
        ├─────────────────────────────────────────────────────┤
        │  L4 特征技能与规划   EigenSkillMap（SVD 特征选项）    │
        │     + Krylov 前瞻 + 对偶探索预算 + minimax 稳健化     │
        ├─────────────────────────────────────────────────────┤
        │  L3 三信号评估器     r(惊讶) g(可控) s(进度对齐)      │
        ├─────────────────────────────────────────────────────┤
        │  L2 子空间世界模型   SubspaceModel（四子空间+进度轴） │
        │     + RidgeLinear 在线转移拟合 + 周期检测(FFT)        │
        ├─────────────────────────────────────────────────────┤
        │  L1 谱压缩           块级特征 φ、增量 SVD、不变量掩码 │
        ├─────────────────────────────────────────────────────┤
        │  L0 感知场           PerceptionEncoder + Φ 场(已有)  │
        └─────────────────────────────────────────────────────┘
                     ▲                         │
                     │ CEAX 假设-实验闭环       ▼
        ┌─────────────────────────────────────────────────────┐
        │  执行层：SpectralCeaxController → GameAction          │
        │  （ScriptBank / Coverage / Arrange / Keyboard 全保留）│
        └─────────────────────────────────────────────────────┘
```

**与既有三件套的统一**：
- **Φ 场 / 泡壁**（`core/phi.py`，灵境-场架构 PDF）= L0 的能量泛函：泡壁 = 高 ∇Φ 处 = 效应椭球边界 = SSA 最该采样之处。两者在数学上同构：Φ 密度是块特征的 0 阶统计，SSA 的 V_k 是其 2 阶谱统计。
- **CEAX 闭环** = L2 的**主动辨识协议**：每个假设是一次线性约束的证伪尝试；实验设计准则是"最大化 E 的秩增益"（Ch2 消元失效的逆命题）。
- **ScriptBank** = L5 的**显式列空间**：已验证脚本是以坐标形式存储的能力基向量；跨游戏迁移 = 通过 `MatrixMemory` 的行空间先验warm-start。

---

## 4. 与世界最先进研究的对齐（2024–2026）

| 前沿方向 | 代表工作 | SSA 的对应与差异 |
|----------|----------|------------------|
| ARC-AGI 冠军方案：测试时训练（TTT）+ 数据增广 + 采样搜索 | MindsAI（Jack Cole/Mohamed Osman，2024 榜首）；the ARChitects（2024 冠军，53.5%） | TTT 的本质是"每任务在先验上做少量梯度步"。SSA 用**岭回归闭式解**作为 TTT 的凸代理（无 GPU 可行），共享基 V_k 即"预训练先验"，每局的在线拟合即"测试时适配"。路线 E3 给出完整 TTT 循环 |
| 程序合成 + 自我改进 | DreamCoder；CodeIt (MIT, 2024)；HRL 引导 | 脚本=程序；A=CR 分解给出程序库的秩结构；谱验证器负责证伪（对应 DreamCoder 的版本空间剪枝） |
| 归档式探索 | Go-Explore（Uber AI） | 状态投影到子空间坐标作 archive key；"return-then-explore"由 ScriptBank 回放 + 未尝试边规划实现（已有 `offline_level_bfs.py`） |
| 特征选项 / 能权探索 | Klyubin & Polani (empowerment)；Gregor et al. eigenoption discovery | `EigenSkillMap` = 转移矩阵 SVD 的左奇异向量 = 特征选项（eigenoptions）。与深度版差异：无需策略网络，闭式可得 |
| 后继特征迁移 | Barreto et al. successor features | `MatrixMemory` 行空间 = 后继特征的可迁移核；最小范数解 [S6 §4.5] 防止迁移幻觉 |
| 世界模型 RL | DreamerV3；MuZero | SSA 是其**显式线性算子**退化：以可辨识性换算力。MuZero 的隐式价值 = SSA 的进度轴 p* + Krylov 深度 |
| 生成式交互环境 | Genie 2 (DeepMind) | 路线图 E4：谱聚类对象 + 槽位世界模型 = 可控生成的前置 |
| 主动推断 | Friston 自由能 | §1.2：岭回归 = 线性高斯自由能；探索 = 残差采样；利用 = 进度对齐。理论完全同构 |
| 状态空间模型 / 线性记忆 | S4 / Mamba / HiPPO | HiPPO 的本质 = 把历史投影到正交多项式基（S6 §8.3.4 勒让德/切比雪夫！）。SSA 的增量 SVD 缓冲区 = HiPPO 的有限维退化，是同一家族的记忆算子 |
| 开放式进化 | POET；minimal criterion coevolution | 课程 = det(Gram) 体积增益最大化任务流（§7 E5） |
| LLM 慢思考 | o1/DeepSeek-R1 式测试时推理 | LLM = 高层假设生成器（System 2 提案），SSA = 快速可证伪验证器（System 1 检验）。LLM 每个建议被转成线性约束/脚本，由效应矩阵 E 实验裁决 —— 推理算力只在裁决处消耗 |
| ARC-AGI-3 专用 | Tycho（NIMI）、官方 ReasoningAgent | Tycho 类"逆向工程 + 脚本固化"= SSA 的显式列空间路线（ScriptBank 已实践）；SSA 补上**在线谱内核**使未知游戏也能自动构建同种结构 |

**结论**：各前沿路线在 SSA 框架里都能找到自己的线性代数坐标 —— 这正是"用最基础数学统一最前沿研究"的含义：不是发明新数学，而是**把每个 SOTA 组件翻译回子空间语言后，它们的组合方式自动显现**。

---

## 5. 形式化规范

### 5.1 符号表

| 符号 | 含义 | 实现 |
|------|------|------|
| φ(x) ∈ R^{2B²} | 网格块特征（B×B 块，每块：占用率 + 颜色多样性） | `lincore.features.encode_grid` |
| Δ_t | φ(x_{t+1}) − φ(x_t) | 效应向量 |
| E ∈ R^{m×d} | 效应矩阵（行堆叠的 Δ） | `SubspaceModel` 缓冲 |
| p* ∈ R^d | 进度方向：w = argmin‖Ew − y‖² + λ‖w‖²，y=关卡进度指示 | `SubspaceModel.progress_axis` |
| S ∈ R^{B²×K} | 区域-动作效应量矩阵 | `EigenSkillMap` |
| r(Δ) | ‖(I − V_k V_kᵀ)Δ‖/‖Δ‖ | `SubspaceModel.novelty` |
| g(a) | ‖E_a‖_F²/n_a | `SubspaceModel.action_gain` |
| s(Δ) | ⟨Δ, p*⟩/(‖Δ‖‖p*‖+ε) | `SubspaceModel.progress_alignment` |

### 5.2 关键命题（本仓库实现的保证）

- **P1（压缩最优）**：V_k 取自 E 的 top-k 右奇异向量 ⇒ 截断误差 ‖E − E_k‖_F 最小（Eckart–Young [S6 §7.1.2]）。
- **P2（不变量可靠）**：块坐标 j 属于 N(E) 当且仅当其在所有 m 个样本上的能量 Σᵢ Eᵢⱼ² = 0（数值上 < τ·max 能量）。所得掩码是**保守的**：只排除确证不变的坐标，绝不误杀活坐标（假解防护）。
- **P3（迁移不幻觉）**：补全先验取最小 Frobenius 范数的秩-k 解（迭代奇异值硬阈值），即旧数据张成子空间内的最小扰动插值 [S6 §4.5.2 广义逆]。
- **P4（预算可行）**：全部在线量为 O(d) ~ O(d²)（d=128 或 B²=64），单步决策 < 5ms CPU。

### 5.3 决策融合律

点击目标综合评分（ SpectralCeaxController 注入）：

$$\text{score}(o) = 0.40\,\hat g(\text{region}(o)) + 0.35\,\text{untried}(o) + 0.25\,s^+(\text{block}(o)) - 0.15\,s^-(\text{block}(o))$$

键盘动作综合评分：

$$\text{score}(a) = \text{epistemic}(a)\cdot 1.2 + \text{ema}(a) - \text{loop}(a) + 0.30\,\hat s(a) + 0.20\,r(\widehat\Delta_a)$$

其中 $\hat s(a)$ 是岭回归预测的进度对齐、$r(\widehat\Delta_a)$ 是**预测效应的新奇度**（预期信息增益的代理：走向未知方向的动作获得加分）。

---

## 6. 实现映射（本次落地）

| 文件 | 理论组件 | [S6] 章节 |
|------|----------|-----------|
| `lingjing_solo/lincore/features.py` | 块特征 φ、像素活跃掩码 | Ch1 |
| `lingjing_solo/lincore/subspace.py` | 四子空间世界模型、进度轴、三信号 | Ch3, Ch4, Ch7 |
| `lingjing_solo/lincore/spectral.py` | 稳定岭回归（SVD 解）、增量正规方程、周期检测、Krylov 前瞻 | Ch4, Ch6 |
| `lingjing_solo/lincore/eigen.py` | 特征选项（SVD eigenoptions）、区域杠杆 | Ch7 |
| `lingjing_solo/lincore/gradient.py` | SGD+动量逻辑回归、对偶预算调度、minimax 稳健秩 | Ch9 |
| `lingjing_solo/lincore/memory.py` | 跨游戏低秩补全迁移（迭代奇异值硬阈值） | Ch7, Ch4 |
| `lingjing_solo/lincore/ttt.py` | **E3 谱-TTT**：转移重放库、共享基（全局 PCA）、QR 合并行空间、世界新奇度 | Ch7, Ch4.4 |
| `lingjing_solo/lincore/hypothesis_lab.py` | **E2 假设实验室**：LLM JSON→假设、区分实验设计、观测解释率裁决 | Ch2, Ch1 |
| `lingjing_solo/lincore/segments.py` | **E4 谱聚类对象分割**：共激活签名、归一化割（Fiedler 向量） | Ch7, Ch6 |
| `lingjing_solo/lincore/mind.py` | SpectralMind 编排器（L1–L5 集成 + 热启动 + 重放入库） | 全部 |
| `lingjing_solo/transfer/spectral_layer.py` | SpectralCeaxController（CEAX 闭环嫁接 + 假设实验注入） | §3 |
| `agent/spectral_agi_agent.py` | Kaggle 提交入口（无插件、纯未知能力） | §3 |
| `scripts/bench_spectral_vs_ceax.py` | 谱 vs 基线对照基准 | §8 |
| `scripts/train_shared_basis.py` | E3 离线重训（重放 → 共享基） | §7 |
| `tests/test_lincore.py` | 合成动力系统验证（已知真值，43 项） | §7 |

设计原则：**纯 numpy、无新依赖、异常全护栏**（谱内核任何异常都不得击落 CEAX 决策链 —— 沿用 `my_agent.py` 的护栏风格）。

---

## 7. AGI 进化路线图（自进化闭环）

- **E1 ✅（2026-09-06）谱内核在线**：单局内四子空间 + 三信号 + 特征选项生效。
  验证：43 项单元测试全绿；lp85 实局 12 步通关 L1；6 游戏×300 步对照基准
  与 ceax 基线持平无回退（各通 4 关，aggregate 1.634 vs 1.508，
  `ui/static/bench_ssa_comparison.json`）。
- **E2 ✅（2026-09-06）假设实验室**：`lincore/hypothesis_lab.py` —— LLM JSON 建议
  → 结构化假设（动作×坐标×预期块集）→ 区分实验设计（消元判据贪心版）→
  谱裁决（观测解释率 ≥0.5 判支持，两次支持即"已验证"，可入 ScriptBank）。
  已接入 `SpectralCeaxController`：待验证假设优先占用实验点击预算；
  `auto_from_mind` 在无进度时把谱信号升级为显式假设（LLM 缺席的 System 2）；
  agent 侧 `SSA_LLM_CONSULT=1` 时在卡点（第 60/240 步无进展）向 LLM 有限次
  征集假设（`register_hypotheses` 入口，故障全护栏，默认关闭）。
- **E3 ✅ 实现 / ⏳ 增益待证**：`lincore/ttt.py` + `scripts/train_shared_basis.py`。
  对局设 `LINCORE_REPLAY_PATH` 自动重放入库 → 离线全局 PCA 拟合共享基 →
  下局设 `LINCORE_SHARED_BASIS` 自动热启动；新奇度升级为对
  「本局行空间 ∪ 世界先验行空间」QR 合并基的残差，并经探索门进入决策
  （**只奖不罚**：效应被先验解释 ≠ 无生产力，已由 r11l 事件校准）。
  实证（2026-09-06）：25 游戏录得 4171 条转移 → k=12 共享基 → 热启动复测
  aggregate 0.598/5 关 vs 基线 0.659/6 关；逐局仅 r11l 翻转，且该局在
  **两种配置**下 4 次运行仅 1 次通过（游戏实时动画导致固有方差）——
  结论：**该方差量级下 A/B 不可判定**；热启动保持环境变量门控（默认关闭），
  待 Kaggle 私有局或多重复实验给出足够样本再定去留。
- **E4 ✅（2026-09-06，分割件）/ 进行中（槽位世界模型）**：`lincore/segments.py`
  共激活签名 + 归一化割（Fiedler 向量）实现块级对象分割；接入反事实引擎做
  对象级操作待做。
- **E5（规划中）开放式课程进化**：任务生成器采样使 det(Gram(V)) 边际最大的
  变体任务；POET 式最小准则 = "进度轴可辨识且未饱和"。

进化判据（可测量）：跨任务平均首次通关步数单调下降；共享基维度 k* 随任务数对数增长而解释方差不降（子空间在"变厚"而非"变杂"）。

---

## 8. 验证协议

1. **单元（本次）**：合成动力系统已知真值 —— 不变量恢复、进度轴方向、周期检测、可分性学习、特征选项命中真按钮、低秩补全误差、对偶预算有界、CEAX 集成冒烟（`tests/test_lincore.py`）。
2. **回归**：既有 `tests/test_transfer_l6.py` 必须全绿（谱层只加不改语义）。
3. **本地摸底**：`play_local.py --game lp85,sb35,...` 对比 ceax_unknown_agent 基线（关注实验命中率的提升与重复点击的减少）。
4. **Kaggle**：`agent/my_agent.py` 保持 P2 主线不变；`spectral_agi_agent` 经摸底后作为 unknown 能力主入口替换 `ceax_unknown_agent` 提交。

---

## 9. 局限与诚实声明

- 线性算子是真实动力系统的**一阶局部近似**；对强非线性机制（组合锁逻辑）靠"脚本 + 区分实验"离散化处理，谱内核提供的是注意力与证据权重，不是全知模型。
- 进度轴 p* 依赖 levels_completed 信号的密度；对长前期游戏，前 k 步退化为"新奇+杠杆"纯探索 —— 这是设计使然（诚实探索优于伪造进度，见 FALSE_SOLUTION_CHECKLIST）。
- 本白皮书是**架构理论**而非"AGI 已实现"声明。每个 E 阶段都配可证伪的验收指标。

---

*引用格式：Lingjing Team, "SSA：谱-子空间认知架构理论白皮书 v1.0", ARC Prize 2026 工作区文档, 2026-09.*
