# 统一 R3 设计：兼容全部比赛的搜索型规划层

> 日期：2026-09-18
> 目标：把现在散在 8 个文件里的"R3"收成**一个内核**，既不让 LS20 / AR25 / R11L 的既有成绩回退，又能吃到没写过专用代码的新游戏。
> 依据：本文每条约束都带 file:line 证据；未验证的推断会标「假设」。

---

## 0. 一句话结论

R3 必须是**双层搜索**（配置层 → 动作层）跑在**一个内核**上，游戏差异全部下沉到一个声明式 profile（状态键 / 资源通道 / 动作候选生成器 / 进度信号），并且内核只能建立在**画面 + step()** 这一套线上可得的信息上——今天最能打的几个"R3"（快照回滚搜索）在线上根本不可用，它们的正确身份是**离线解法工厂**。

---

## 1. 三条硬约束（决定架构，先摆出来）

**C1 线上拿不到引擎对象。**
`arc_agi/remote_wrapper.py`（`RemoteEnvironmentWrapper`，第 17 行起）里没有任何 `_game` 属性，只有 `reset()`(79) / `step()`(139) 走 HTTP POST；`_game` 只存在于 `local_wrapper.py:69`。
⇒ 凡是靠 `env._game` + `set_level()` + `snapshot/restore` + `perform_action(raw=True)` 的实现都不能上线：
`C:\newtask-pi\tools\ls20_solve_v4.py`、`arc_adaptor/arc_shadow.py:41`、`run_ar25_r234.py` 的 `r3_state_search:361` / `r3_beam_search:411`、`ft09_solver.py:169` 的 reset+重放 BFS。

**C2 线上每一步都不可撤销，且错了要掉计数器。**
唯一可回的分支信号是 `FrameData`（`arcengine/enums.py:131-140`）：`state / levels_completed / win_levels / full_reset / available_actions`。
⇒ 线上"搜索"= 在**学到的/自建的**转移图上预演，落地一步、核对预测、错了就重规划。这正是团队自己那份 `docs/搜索算法和胜利检测.pdf` 写的路线：用 WorldModelField 的 `(s,a)->s'` 建图，步代价 1，成环代价 ∞，BFS/Greedy-Best-First 找路径。
⇒ 例外：`available_actions` 含 ACTION7(UNDO) 的游戏可以真回滚，那是线上版的 snapshot/restore（见 B4）。

**C3 不要在最差的空间里搜。**
`docs/ar25-pruning-spec-from-arcsage.md` 的 0 节已经用血泪写过："不要在 ACTION1–5 的动作树上硬 DFS/Best-First 冲 L7"，Lingjing 旧报告的贪心 Best-First（300s / 60 万节点超时）就是"在错误搜索空间里烧预算"；ARC-SAGE 可复现的路径是**先枚举终态配置**（块坐标 × 镜轴坐标）→ 反射闭包判覆盖 → 再反推短路径。
⇒ 这就是"双层"的由来，也是它必须写进内核、而不是只写在 AR25 里的理由。

预算事实：提交模板 `agents/templates/lingjing_solo_agent.py:46` 是 `MAX_ACTIONS = 800`；`PLAYBACK_FPS = 5` 只是节奏不是硬限（`agents/agent.py:265-268` 只做 sleep）。远端 HTTP timeout 10s。⇒ 单步规划按 1–2s 限流，全局按"剩余动作数 + 剩余墙钟"双闸门。

---

## 2. 现状盘点（收编对象）

| 实现 | 搜索空间 | 状态键 | 剪枝 | 上线可用 |
|---|---|---|---|---|
| `newtask-pi/tools/ls20_solve_v4.py` | 真引擎动作树 | 玩家xy+三元组+已吃目标+敌人快照，**不含步数计数器** | noop + 计数器支配 + 掉命 | ✗（C1） |
| `arc_adaptor/arc_shadow.py` | 真引擎动作树 | sprite 位姿/像素+选中+2 个引擎字段，不含计数器 | visited + 预算 + GAME_OVER + `next_level` 阉割 | ✗ |
| `run_ar25_r234.py` | 目标分解(配置) + 动作树兜底 | `_state_key(g)` | visited/预算/beam 宽/组合数上限 | ✗ |
| `ar25_solver/solver.py` | **纯 Python 模拟器** | 配置键，不含 `steps_used` | Dijkstra + 预算界 `d+h>budget` + `sim.lost` | △（模拟器是按游戏手写的） |
| `world_model/field.py` + `planning/search.py` | 学到的 hash→hash 转移图 | `hash_grid(grid, levels)`，计数器不入键 | visited + majority 边降权 + noop_streak + 失败计数 | ✓ |
| `planning/search/{astar,bfs,dfs}.py` | 回调注入，通用 | 调用方给 | Dijkstra/visited/深度 | ✓（**目前无人调用**） |
| `planning/ls20_solver.py` | 像素 5 格点 BFS，逐步重规划 | 只有格子坐标 | avoid 陷阱集 + 重试圈数 | ✓ |
| `r2r3r4_combo.py` / `run_ls20_r2r3r4.py` | 像素 A*（4 份近似重复实现） | 帧 sha256 | visited + 循环惩罚 | ✓ |

三件要修的家底问题（都威胁"兼容"）：
1. `arc_adaptor/agents/strategies/__init__.py:4-10` 导入 `.ar25/.base/.generic/.ls20/.r11l/.registry`——这些文件在本仓库不存在，只有 `sync_to_arc.sh` 拷进 ARC checkout 后才成立。集成测试必须在 ARC 侧跑，Lingjing 侧绿不代表能上线。
2. 解法数据有两份且已漂移：`script_bank._LS20_LEVEL_ACTIONS` L7 = 54 步（合计 310），`newtask-pi/data/ls20_sols_v2.json` L7 = 53（合计 309，与线上 Scorecard 一致）。第 53 步就 WIN，多的那个是死动作。
3. 判游戏靠猜：`ls20_solver.py:498 looks_like_ls20()` 用像素猜；`transfer/families.py:10-24` 用 id 前缀猜家族。新内核需要一个显式的 profile 选择步骤。

---

## 3. 设计

### 3.1 分层决策顺序（保证不回退）

```
T0 罐头路线      CannedRoutePlugin / script_bank        已验证解优先，命中即用（LS20 309 / AR25 276）
T1 专用在线规划   LS20View 等 profile 的 path 求解        保住 L1-L2 这类已能在线过的关
T2 通用 R3 内核   配置层搜索 + 动作层搜索（本文主体）      新游戏的主路
T3 探索兜底       advisor / explorer 信息增益             零分风险的对冲
```
上一层只在"有把握"时短路；任何一层通关就 `full_reset`/换关并重新选层。**T2 追平 T1 之前不许退役 T1。**

### 3.2 内核协议：`WorldView`（每游戏一份声明，唯一允许的游戏知识）

```python
class WorldView(Protocol):
    # —— 观察 → 抽象状态（禁止把资源计数器放进键，支配剪枝才成立）
    def grids(self, frame) -> list[np.ndarray]: ...        # frame.frame 是 list：见过 (1,64,64)/(2,64,64)/(6,64,64)/(17,64,64)
    def key(self, grids) -> Hashable: ...
    def counters(self, grids) -> dict[str, int]: ...       # 单调不增通道：剩余步/生命…，可为空
    def proposals(self, grids, key) -> list[ActionSpec]: ...# ★ 动作候选生成器（见 3.3）
    def config_space(self, grids) -> ConfigSpace | None: ...# ★ 配置层（见 3.4）；None 表示只有动作层
    def score(self, grids) -> float: ...                    # 启发式/目标分，可退化
    def progress(self, frame) -> int: ...                   # 默认 = frame.levels_completed（通用，零分支）
    def done(self, frame) -> bool: ...                      # 默认 = frame.state is WIN
    def dead(self, frame) -> bool: ...                      # GAME_OVER 或某个 counter 掉了
```
`progress/done` 的默认实现直接吃 `FrameData` 的官方字段——这是"兼容所有比赛"的地基：**进度和胜利不需要认识游戏**。PDF 里"输出网格 == 目标网格"式的判据保留为可选第三信号（用于 `levels_completed` 不动的游戏）。

### 3.3 动作候选生成器（统一 LS20/AR25/R11L 三种交互范式）

分支因子是统一 R3 的真敌人：LS20 是 4 个方向，AR25 是 4 方向 + SELECT + 镜轴位置，R11L/VC33 是 `ACTION6(x,y)`，定义域 64×64 = **4096**。
⇒ 内核**永不**枚举 `available_actions × 参数域`。profile 必须交 `proposals()`，其语义是"从画面提出的、值得试的少数动作"：
- LS20：4 邻居格（`DIRS` + `_walkable`，已有）
- AR25：`enum_useful_positions`（`run_ar25_r234.py:89`）——把块推到目标/反射位
- 点击类：连通域质心、UI 元素中心、上一轮有效点击的邻域（`exploration/click_sweep.py` 已是这个思路的采样版）
候选质量决定一切，所以 `proposals()` 要带**在线自我扩展**：某动作有效但候选里没有它 → 记进该 profile 的候选表（对应 `transfer/` 的技能库），下一层搜索就有。

### 3.4 配置层优先（把 arcsage 的教训固化）

有 `config_space()` 时先搜配置：`目标终态 = (各块位姿, 各轴坐标, …)`，判据是覆盖/匹配谓词（AR25 是反射闭包 `covered ⊇ targets`；LS20 是三元组 `(shape,color,rot)` 相等）。配置解出来后交给动作层做 **pathify + 校验**：
- pathify：在学到的图/模拟器上做 BFS 求实际步序列（AR25 的 `_generate_actions` 是手写的推广版）
- 校验：arcspacesage 规格点名的坑——纯几何枚举会给出因碰撞不可达的配置（AR25 L7 理想 `(8,1)`，实际赢在 `(8,11)`）。所以配置解**必须**经过"真执行一次"的验证才算数。
没有 `config_space()` 的游戏直接进动作层，此时 noop + 计数器支配两条剪枝是唯一的空间压缩手段，必须默认开启。

### 3.5 剪枝契约（内核写死一次，四条）

| 名字 | 判据 | 现有同物 |
|---|---|---|
| P1 noop | `key(child) == key(parent)` → 丢 | v4 `if k == akey(s)`；`field.py:188 noop_streak`；`advisor.py:66 "noop_effect"` |
| P2 计数器支配 | `best[key]` 存该键见过的最大 counter；`child.counter <= best[key]` → 丢；出堆再核一遍 | v4 `best_counter`；`simulator.py:243` 键不含 `steps_used`；`field.py` 键不含步数 |
| P3 死亡 | `state==GAME_OVER` 或 lives 通道下降 → 丢 | v4 `if g.aqygnziho != s[6]` |
| P4 预算 | `g + h > budget`；budget 来自 counters 或剩余动作数 | `solver.py:170`；`r234` 的 `>= ilqnjlrnkk-1` |

P2 的前提是"键里不含计数器"，这条要写成内核断言（profile 若把计数器塞进 key 就直接报错），否则支配剪枝会静默失效——这是最容易犯、最难查的错。

**断言的实证依据（2026-09-19 在 AR25 引擎上量得，见 `arc_adaptor/arc_shadow.py:157-192`）。** `_state_key` 是 `_snapshot` 的**有意掩码投影**，不是原样拷贝：

- 掩掉 `lelsvjlwneo.current_steps`。AR25 存在真实的状态等价：L7 那块 7×3 的 `0006lxjtqggkmi`，`np.rot90(px,2)` 的**数组与字节都等于原数组**，且 `tvqhikkvjs`（`environment_files/ar25/0c556536/ar25.py:1295-1306`）的重定心让转 180° 后 `(x,y)` 也回到原值 ⇒ "转半圈"是伪装成动作的 noop，只消耗一步。计数器若进键，这种状态每步都"新"，noop/支配两条剪枝同时失效、搜索原地打转。**"键不含计数器"不是洁癖，是 P1/P2 的成立条件。**
- 掩掉 `ovoizfolxfq`（每块到轴的距离表）。它是纯缓存：`vrkougbfel`（同文件 1287-1293）= `abs(精灵中心 − 轴坐标)`，两侧都已在键里，掩掉零信息损失。⇒ arcsage 规格那句"state_key 必须含旋转距离表"**不需要显式字段**：旋转落在 `pixels`+`x,y`，距离落在两者之差。
- 掩掉 `hujpxmlafgh`（赢了等跳关）与 `_state`：终态走 `solve_level` 的显式判定（313/317 行），不混进等价类。
- 掩掉 `xukxeewuexo` / `xjwpeqpcxav`：step() 开头对非 ACTION5 每次重置（1699-1701），属动作派生量而非状态。
- 留在键里的 `hsiusrsrdkswnt`（L2 教程动画闩）看着像该掩的相位，实测无碍：`perform_action` 内部 `while not is_action_complete(): step()`（`arcengine/base_game.py:222-234`），动画 8 帧期间 `step()` 自己 early-return，搜索取到的恒为静止值 {0, −1}，等价于一个 one-shot 位。内核仍应要求 profile 只登记**静止态**标量。

`arc_shadow` 的一处差距要记着，别当范本抄：它的键已经不含计数器，但**没有**做成 P2，只有一个 push 前写入的 `seen`（320-322 行）配 `h*10+len(path)` 的强启发优先级——先占坑的路径不保证省步，与它"不追求最短"的目标自洽，但谁要拿最短解必须换成 `best_cost[key]` + 出堆判定（内核按 P2 实现即可同时满足两者）。

### 3.6 资源通道自动发现（新游戏没有 HUD 知识时的出路）

`counters()` 不该要求人先去读每个游戏的 HUD。通用做法：**挖掘单调通道**——观察所有画面派生标量（某行色块连续段长度、某色像素计数、某区域精灵数），保留"跨步单调不增、且偶尔下降"的那些，作为 P2/P4 的通道。
已验证实例：LS20 步数条 = 第 61 行 x∈[13,56) 的色 11 连续段，右端给出剩余与上限（`ls20_solver.read_step_bar`，今晚实测驱动了 L2 的补给绕行并第一次在线过 L2）。自动发现器要能把它当"发现出来的通道"重新推出，才算通用。

### 3.7 后端（`expand()` 的四种实现，内核不关心）

- **B1 EngineBackend**（离线专用）：真引擎快照/回滚 = 今天的 v4 / arc_shadow。用途：生成与更新 T0 罐头解、当回归基准。产物仍写进 `script_bank`。
- **B2 SimulatorBackend**：纯 Python 自建模拟器（`ar25_solver/simulator.py` 已存在）。最省线上步数，但按游戏写，属 profile。
- **B3 LearnedGraphBackend**：线上默认，`field.predict_graph`。带不确定度：majority_ratio 低的边加惩罚，走错就重规划并把纠错边回写。
- **B4 UndoBackend**：`available_actions` 含 ACTION7 时，真执行 + UNDO 即线上合法的回滚 ⇒ 把 B1 的搜索搬到线上。假设：仅部分游戏有，需按 C2 的 `available_actions` 探测，不可写死。

  **B4 前置判据（2026-09-19 修正，原写法过强）：undo 恢复的状态维度必须 ≥ 本游戏 `key()` 的维度，否则 B4 会给出"以为回滚了"的假解。** AR25 就是反例：`siizypdewv`/`neimtgmbvs`（`environment_files/ar25/0c556536/ar25.py:1309-1327`）的 undo 快照 `flqblmrxsla` **只存块与轴的坐标**——不恢复 `pixels` 旋转、不恢复选中对象 `yvifanjrcyu`、**也不退还 `current_steps`**。而它的 `_state_key` 含 pixels 与 `_sel_idx` ⇒ 在 AR25 上按 ACTION7 当回滚，搜索会拿着被污染的计数器/旋转态继续推。
  ⇒ B4 的启用条件要写成一次探测：进某游戏前，先在本地/影子上做一次 `act → undo → 比对 key()` 是否回到原值；只有回到才允许线上用 B4。探测失败 ⇒ 退 B3，且把"该游戏 undo 不等价"记进 profile。
  同源提醒：`arc_shadow._snapshot` 自己也没覆盖 `flqblmrxsla` 与 `_current_level_index`（靠 285 行阉割 `next_level` 兜住，`neutered` 失败即失守），所以 B1 也不是无条件可信的。

线上一层只允许 B3/B4；B1/B2 属离线工厂。这条边界要在代码里用 `is_local` 断言，防止"离线能跑、上线静默退化"。

### 3.8 现状对账：内核仍未统一（2026-09-20，①②③ 之后）

§3.1–§3.7 描述的"一个内核 + 每游戏一份声明式 profile"**目前只有文档，没有代码**：`grep -rn WorldView --include=*.py` 在仓库内 0 命中。①②③ 统一的是三个**接口层**，不是内核——(a) §3.2 plan 出口（`plan_contract`，两个运行器各 import 一次：`run_ls20_r2r3.py:56`、`run_ar25_r234.py:42`）；(b) R2 观测的**契约形状**（`r2_ls20.py` 351 行 / `r2_ar25.py` 253 行，两份互不相干的实现，只是都出 observation / `state_hash` / `transition`）；(c) §5 证据 schema（`evidence_compat` 共享，但落盘方式两份：LS20 把 `ev.build_tick` 摊在主循环 `:447`/`:496`，AR25 走 `tick_trail.TickTrail`）。

搜索本体与全部外围机制仍是各一套，成对重复的有：合法动作集 `_legal_names:78` vs `AR25_ACTION_NAMES:60`；回放校验 `_replay:168` vs `run_names:76` + `_verify_and_return:851`；预算钳制 `_budget_of:175` vs `_pos_int:295`；plan 组装 `plan():304` vs `ar25_plan():306`；搜索 `r3_position_astar:102`（手写 heapq）+ `r3_joint_bfs:185`（手写 deque）vs `solve_goal_decomp:368` / `solve_goal_decomp_dual:528` / `r3_state_search:617` / `r3_beam_search:667`；连证据路径助手 `_rel_to_repo` 都是 `:37` 与 `:164` 两份。§3.5 那四条剪枝契约（P1–P4）没有任何落点，P2 要求的"键里不含计数器"这条断言也没人检查。

对 7.5 那张表的影响（表本身是 2026-09-19 的审计快照，按 §10 不改写，此处只做增量说明）：①②③ 勾掉的是"统一输出九字段""plan 引用输入 state hash""planner 只返回 abstract name""候选动作属合法集合"（AR25 侧从"完全不校验"变成 `record:127` 当场拒 + `actions_legal` 判据）三行半，"消费 R2 的 observation + 状态哈希"从"否：无 R2 层"变成"是，但两份实现"。**两行仍未勾**：`经 registry 按 game_id 选 planner`（registry 确实存在并按 game_id 路由（`registry.py:20-28`），但路由到的是 `_LEVEL_ACTIONS` 罐头（`ar25.py:18`、`ls20.py:13`），两份策略文件里 planner / `plan_contract` 0 引用；两个 R3 运行器仍是 `__main__` 脚本，不经 registry。也就是说缺的不是"有没有路由"，而是路由的**末端接的是罐头而非 planner**）与 LS20 的决策权（G2 关罐头下 97 tick 全走 `r3_ls20_solver`+`r3_layout_wait`，A*/joint 0 次接管）。所以 7.5 那句"统一的 R3 还没做到"自 ③ 之后依然成立，只是缺口从"契约与算法各在一半身上"收窄成"内核没有实现、路由没有接"。**推论（本轮定下的排序依据）**：在这两行补齐之前抽内核，得到的会是一个没有真实调用方的框架——验收时内核仍然不被执行，等于用文档换文档。

另有一条本轮复核出来的记账缺陷，直接关系到怎么读 8.11 的哈希数字：`tick_trail` 的双向审计在 `record:127` 里无条件累加，而 `abort_attempt:119` 只把行丢掉、**不回退计数器**，于是 `hash_split_*` 混了"已提交"和"已回滚的搜索帧"两类段落。实测口径：本轮 `report.json` 记 `hash_split_semantic_only=15`，而对 294 行**已提交**转移逐行复算 `before_engine_key != after_engine_key`，得 **0/294**（全部 294 条都是 moved 且搜索键跟着变）——那 15 次只可能出现在被回滚的搜索展开里。这不改 `state_hash_not_coarser_than_search_key` 的结论（判据看的是反向计数，两个方向都仍是 0 欠收），但**在计数器按段落拆开之前，"新哈希比搜索键更细 15 次"只能读作"搜索帧里发现了 15 次搜索键碰撞"，不能读作环境侧证据**（§10 那条区分对审计计数同样成立）。同时这也提示：3.5 掩码依据（`:113` 说 `ovoizfolxfq` 是纯缓存、掩掉零信息损失）与 8.11 给正式哈希的理由（旋转距离不进搜索键）之间存在一处需要对账的张力，需要一次字段级归因才能定谁对——拆计数器的同时把那 15 例的**变化字段**记进 `audit.examples` 即可判掉。修口的范围是一次三处，不是三件事：`tick_trail` 按段落拆计数、把变化字段记进 `audit.examples`、以及 `run_ar25_r234.py:1169` 那句照抄计数的 `limitations` 文案（"语义变/搜索键没变 15 次（新哈希更细）"现在也在机器可读的 `report.json` 里，不只在本节）。**本轮按定下的口径只登记、不动码。**

---

## 4. 落地阶段与验收

| 阶段 | 内容 | 验收（全绿才进下一阶段） |
|---|---|---|
| P0 | 定契约：`WorldView` / `R3Kernel` / 四后端签名 / 分层顺序；把 `planning/search/{astar,bfs,dfs}.py` 接成内核的可复用件 | 纯单测；用现成 recording（`ARC-AGI-3-Agents/recordings/*.jsonl`，含 309 条那份）造 key/noop/支配用例 |
| P1 | LS20 迁进内核：`_walkable/_bfs/_pad_exit_only/read_step_bar/find_refill_cells` → `Ls20View`；**删** `_estimate_rot_entries`、`_estimate_color_entries`、`_l2_trap_cells`、`_preferred_pad_toggle` 四处按关硬编码，踩台次数由内核搜出 | (a) T0 重放仍 7/7=309；(b) 关罐头时 T2 ≥ 现在 T1 的 2/7，且必须过 L3（今晚 L3 卡在 pad 顺序，正是硬编码的锅） |
| P2 | AR25 内核化：`config_space()` = 配置枚举，B2 模拟器接上；R11L/VC33 交 `proposals()`（点击候选） | AR25 仍 8/8；R11L 不掉到既有水平以下 |
| P3 | 计数器自动发现 + 多帧输出处理 | 拿 `sc25-635fd71a`（ARC checkout 里有、仓库零处理，6 关）做**零先验**验证：不写 sc25 专用代码，T2 至少过 L1，且判资源全靠发现 |
| P4 | 退役与清理：`looks_like_ls20` 猜测式分派改注册表打分；合并 `verified_solutions.py` 与 `script_bank` 的 L7 漂移；线上跑一轮 Scorecard | 线上 `levels_completed` 不低于离线；文档回写 `docs/STATUS.md` |

**回归闸门（写进 CI，就是"兼容之前的游戏"的机器保证）**
G1 离线重放 T0 全部罐头解 → LS20 7/7、AR25 8/8 必须仍成立（`verify()` 语义：干净新局重放，不许用快照 hack）。
G2 关罐头（把 `script_for_level` 打补丁返回 None，今晚 `run_ls20_solver_online.py` 就这么干）→ T2 关卡数不得低于当前基线。
G3 现有 `arc_adaptor/tests/test_r234_clicks.py`、`test_vc33_r234_e2e.py` 不许红；且必须补一条 **ARC 侧** 集成测试（家底问题 1）。

---

## 5. 风险与取舍（摊开）

- **线上探索要花真步数。** 走错一次就掉计数器，所以内核的目标函数不是"最短解"而是"置信度加权的最短解"；预测不确定的边要收信息税（`advisor.py` 的 hypothesis 扩展可复用）。这是与离线搜索最本质的区别，别照搬 v4。
- **像素状态混叠。** LS20 换关当帧仍渲染上一关，是我在 L2 白花 8 步才定位的坑。通用处置进内核：动作后画面未变 → P1 noop；画面变了但语义键回到旧值 → 判混叠，延迟一步再确认。
- **候选生成器是单点风险。** `proposals()` 提不出正确动作，配置层再准也没用。要保留"有效动作回灌候选表"的通路，并给 T3 探索留预算。
- **多解/等价状态。** 键太粗 → 支配剪枝误杀真解；太细 → 空间不收敛。用 LS20 的键作标定样本（它的键是 7 关都搜得动的最小集）。
- **不做的事：** 不再新增按关卡的 if；不再往 `ls20_solver.py` 里堆第 5 份像素 BFS；不为新游戏写第二份 `_estimate_*` 表。

---

## 6. 需要你定

D1 P1 顺序：先 LS20 内核化（顺手解掉今晚挂着的 L3），还是先做 P0 契约 + B3/B4 让 sc25 尽早进来看兼容性？
D2 T0 罐头优先是否保留为第一层（我倾向保留：它决定线上分数下限，R3 只提上限）。
D3 sc25 是否可以当 P3 的零先验验收靶（它 6 关、本地有环境、仓库零处理）。

---

## 7. 2026-09-19 复跑与核对（只登记事实，未改 solver）

跑测产物已按 `lingjing-evidence-v1` 落盘（`arc_adaptor/evidence_compat.py` + `agents/strategies/run_ls20_r2r3.py`）：
G1 重放 `verdict=PASS` 7/7、309 步；G2（`--no-scripts`）2/7、97 步、`BLOCKED`（终态非 WIN/GAME_OVER，按 §10 不自洽就当没通过）。

### 7.1 L3 到底为什么死（三层，逐层带证据）

1. **`_refill_detour` 把「这一段算不出来」当成「不用绕」** —— `ls20_solver.py:943`：
   `if budget is None or leg is None or budget >= leg + REFILL_MARGIN: return None`。
   实测 G2 从 t88 起 `leg` 恒为 None，可动作数 8→1 一路掉，绕行始终不触发。
2. **`leg` 为 None 的根因是颜色台检测缺可达性过滤** —— `_find_color_pad` 只按曼哈顿距离取最近
   （`ls20_solver.py:228-229`），而 `_find_rot_pad`(191-196)、`_find_shape_pad`(211-214) 都按
   `_reachable_from*` 过滤过。玩家一步走 5 像素（`ls20_solver.py:12-14` 的 DIRS），
   `(x%5, y%5)` 残差类在关卡内永不改变，所以只有同残差的台子踩得到：L3 玩家残差 (4,0)，
   检测给的却是 (28,43)（残差 (3,3)），同屏唯一可达的颜色台是 (29,45)。
   逐关审计（`arc_adaptor/agents/strategies/probe_ls20_pad_residue.py`，拿 7/7 那份 recording）：
   颜色台 4 次命中全部不可达（L3/L4/L5/L6），rot 在 L4 也退回不可达候选。
3. **补上这处过滤后仍 2/7，但死因换了一条** —— 只在 runner 里打桩（`run_ls20_r2r3.py --fix-color-residue`，
   solver 未改）：t85 起 `phase=refill` 真触发、颜色台变成可达的 (29,45)、rot 2/2 后进入 color 任务。
   新的断点是 t96 步数条归 0 ⇒ 引擎**重开本关**（`ls20.py:1983-2005`：`aqygnziho -= 1`、玩家回出生点、
   重画全屏覆盖精灵 `set_scale(64)/set_position(0,0)`），该帧按 5x5 块工作的检测器全部读空
   （`_find_player` 返回 None）→ 目标标记被过滤成空 → `_queue_next` 在 `ls20_solver.py:974-976`
   直接 return → `plan()` 无动作；而驱动层 `reset_level` 补救的条件要求 `not solver.active`
   （`run_ls20_r2r3.py:269-275`，与 `agent.py:234` 同路径），active 仍是 True 所以没救到 → `r3_none`。

**更正本文/我此前的一处误读**：t97 的 `bar=43/43、steps=42` 不是"最后时刻吃到了补给"，是关卡重开
（引擎玩家被搬回出生点、掉一条命、补给格随重开复原）。逐 tick 证据：
`arc_adaptor/agents/strategies/probe_ls20_g2_refill.py`（按 recording 动作序列重放并照常调用 `plan()`）。

### 7.2 steps_left 进 A*/joint：已落地，但成绩不变，原因值得记进 P1

R2 侧补了预算事实：`r2_ls20.observe_state` 的 `game_specific` 新增 `step_decrement`(counter.efipnixsvl)、
`lives`(game.aqygnziho)、`level_restart_flash`(game.ebfuxzbvn)，派生量 `r2.moves_left(obs)`；
R3 侧 A* 加了硬剪枝（已花动作数 > 预算不展开；`cost + h > 预算` 的乐观估计也不展开；缺预算信息时**不剪枝**，
免得把"没数据"当"没解"），joint BFS 从影子引擎逐步读 (steps, lives, flash) 砍掉超支分支——
好处是**补给格不用建模**，影子引擎自己会把步数条加回来。

测量结论（`state/ls20_r2r3_noscript_20260919_132508`）：仍 2/7，且 planner 分布是
`r3_ls20_solver` 95 + `r3_layout_wait` 2，**A*/joint 一次都没拿到控制权**，`joint_pruned_branches` 全为 null。
⇒ 对 P1 的含义：**预算约束放在"兜底层"等于没放**。内核化时步数预算必须是搜索的一等公民
（每个节点带剩余预算、把 npxgalaybz 当可达的预算刷新点），而不是等 Ls20Solver 失手才启用的补丁。
这也印证 §3 的分层判断：步数不进状态键（不影响可达局面），但**必须进代价**。

### 7.3 ARC 同步 / 边界七条规则的核对（本轮只登记，不改码）

| # | 规则 | 结论 | 证据 |
|---|---|---|---|
| 1 | 只同步 `arc_adaptor` 声明的文件 | **违规风险** | `arc_adaptor/sync_to_arc.sh:25` 是 `cp -R agents/strategies/.`，会把 Lingjing 侧 20 个文件（含诊断用的 `probe_ls20_*.py`、离线 runner）整体推入 ARC；ARC 现目录只有 7 个。且 `arc_adaptor/MANIFEST.md:14-19` 声明的 bundle 里没有 `ls20.py/ar25.py/registry.py`，而它们在 ARC checkout 是未跟踪的 `?? agents/strategies/` —— "声明的文件"与"实际在跑的文件"是两套 |
| 2 | 不许覆盖 ARC 原生 `agents/__init__.py`、`agent.py` | 部分合规 | 脚本 24-30 行确实没复制它们；但 43-50 行把 `agents/__init__.py` 印进 `synced_files`（谎报），且没有 `git status`/`git diff` 预检。实测 ARC checkout：`M agents/__init__.py`(+2 注册行)、`M uv.lock`(±2701 换行符 churn)、根目录 40+ 未跟踪 `check_*.py` |
| 3 | 不回提 recording / `.env` / API key | 合规 | 证据产物只落 Lingjing 的 `state/`；manifest 与 recording 只写相对仓库根路径（`_rel_to_repo`） |
| 4 | `choose_action()` 不加游戏分支 | 合规 | `arc_adaptor/agents/templates/lingjing_solo_agent.py:74-103` 走 `strategies.resolve(game_id)` |
| 5 | 抽象动作 ≠ 复杂动作，缺坐标只能走安全 fallback 且记进 evidence | 部分合规 | 同文件 113-120 行 complex 时 `set_data({"x","y"})`，缺坐标走 `{0,0}`；但 `reasoning` 只有 strategy + abstract_action，**没标 fallback_used** |
| 6 | 官方生命周期优先于 Lingjing 内部生命周期 | **违规** | `arc_adaptor/r2_ls20.py:271-273` 的 `state`/`levels_completed` 取自引擎私有 `_state`/`_current_level_index`；实测 `FrameData` 自带 `state/levels_completed/win_levels`。ARC 侧模板反而是用 `latest_frame.state` 的，两条路径口径不一致 |
| 7 | 同步后必须在 ARC checkout 内跑 targeted tests | **未做** | 脚本末尾只有 `git diff --check`；`arc_adaptor/tests/` 那 3 个用例在 Lingjing 根目录必失败（`ModuleNotFoundError: No module named 'tools'`），正是这条规则针对的情形 |

本轮验证状态：`py_compile` 通过；`pytest tests -k "ls20 or evidence"` 8 passed；全量 `pytest tests` 69 passed / 1 failed
（`tests/test_r5_reflection.py::test_agent_falls_back_when_llm_returns_invalid_action`，import 图与本轮改动无交集，属既有失败）。

需要你继续定的：

D4 1/2/6/7 这四处是否一次修完（同步脚本白名单 + `synced_files` 去谎报 + R2 生命周期改 `FrameData` 驱动 + 同步后跑 ARC 内测试），还是先只修 6（口径不一致会污染证据）。
D5 ARC 侧 strategies（`ls20.py/ar25.py/registry.py`）要不要正式纳进 Lingjing 仓库当唯一真源——否则规则 1 的"声明的文件"永远和实际跑的不同步。
D6 P1 内核化时预算做一等公民（见 7.2），那 7.1 的三层修法里，残差/可达性过滤（①②）与闪光帧免疫（③）哪个先进内核。

### 7.4 提交 `f9c6dc7` 之后的复检（改判一处，新增一条接线障碍）

提交范围是 9 个文件 `+2157/−39`：`ls20_solver.py`、`r2_ls20.py`、`paths.py`、`evidence_compat.py`（后两个是上面这些脚本的依赖，此前一直未跟踪，不落进仓库队友拉到就是 ImportError）、`run_ls20_r2r3.py`、两支探针、`tools/ls20_live_strategy_check.py`、本文。**未推送、未合并**：`git fetch origin` 两次 `Failed to connect to github.com port 443 after 21s`，按缓存 ref 算 `HEAD..origin/main` 为 0，而工作树还有 9 个未提交修改，按 §SOP 不在脏工作树上合并。

规则 3 复核：`git show f9c6dc7 | grep -i "scorecard\|api_key\|recordings/"` 的命中全是文档里的散文引用（含本文提到 `ARC-AGI-3-Agents/recordings/*.jsonl` 那句），没有任何 ARC 侧产物入库。本轮跑测在 ARC checkout 生成的 scorecard（`f0aebecd-06ce-4493-8898-27673d616202`）留在 ARC 侧未跟踪状态，符合"引用而不是回提"。

规则 6 **改判**：上一行表格里写成"违规"过宽。`r2_ls20.py` 其实有两个后端——`observe_frame():156`（`backend:"frame"`，从 `FrameData.frame` 的网格推事实，合规）与 `observe_state():259`（`backend:"engine_state"`，读 `_state:273`、`_current_level_index:275`，只给离线 Tier B 用）。所以准确的表述是：**引擎后端不得出现在线上路径**，而当前 `run_ls20_r2r3.py:417,453` 用的正是引擎后端——它本来就是离线运行器，不是线上入口。真正口径不一致的地方仍在：线上 `agents/strategies/ls20.py` 完全不读 R2 产物，只用 `Ls20Solver` 的内部检测。

新增一条**接线的硬障碍**（比"没接"更具体）：两个后端的预算键不一致。`observe_frame` 只写 `step_bar`（`r2_ls20.py:180`），`observe_state` 写 `steps_left`/`step_decrement`（287-292），而派生函数 `moves_left()` 只认后两者 ⇒ 线上若接帧后端，`moves_left()` 恒为 `None`，7.2 那套剪枝会静默失效。好消息是原料在帧里就有：`read_step_bar():56-65` 能从步数条读出 `(剩余步, 上限)`，`read_lives():68-71` 能读生命，缺的只是键归一（外加 `step_decrement` 默认 1）。只有 `level_restart_flash`（引擎 `ebfuxzbvn`）帧侧没有等价真值，线上要用"检测器集体读空 + 步数条回满"近似，而这个近似本身还没验过。

规则 1 的影响面本轮**变大**了：`agents/strategies/` 里多了两支已提交的探针，而 `sync_to_arc.sh:25` 是 `cp -R agents/strategies/.`，一旦执行就会把它们连同 12 支未跟踪探针一起推进 ARC checkout。

规则 7 本轮**手工满足了**：targeted tests 是在 ARC checkout 内用它的 `.venv` 跑的（`71 passed / 3 failed`，且 3 处失败经 stash 差分确认在 HEAD 上逐字复现），并用 `tools/ls20_live_strategy_check.py` 做了生产路径的对照回归（工作树 `7/7 / 309 步 / WIN` vs HEAD `7/7 / 309 步 / WIN`，逐关步数一致）。脚本自身仍只有 `git diff --check`，这条没变。

D4 因此需要拆成两问：**(a)** 键归一 + `moves_left` 支持 `step_bar`（小改，且是"预算进内核"的前置），**(b)** 线上 `LS20Strategy` 改消费 `observe_frame`（口径统一，但要重测线上 LS20 成绩）。**(a) 是 (b) 的前置**，反过来做会让 `moves_left` 在线上永远返回 None 而没人察觉。

### 7.5 `§3.2 R3` 达成度对比（LS20 vs AR25，2026-09-19 审计）

编号约定：本节及 7.5 表格里的 `§3.2` / `§7.1` / `§7.3` 一律指团队规范《最小可运行闭环》的章节；本文自己的 `§3.2` 是 `WorldView` 内核协议（`:67`），**两边撞号**，读的时候按这个约定区分。团队规范目前只在成员本地 `~/Downloads/`，仓库 `docs/` 下没有副本，而 `run_ls20_r2r3.py`、`paths.py:3`、`r2_ls20.py:3` 三处代码注释都写作 "见 docs/最小可运行闭环 §3.1/§3.2/§4.3"——引用指向仓库内不存在的路径，按 §4.3 职责表（schema 与文档归 Lingjing 侧）这份规范应当入库。

| 要求（团队规范 `§3.2` + `§7.1`） | LS20 | AR25 |
|---|---|---|
| 消费 R2 的 observation + 状态哈希 | 是：九字段出口 `run_ls20_r2r3.py:313-331` | 否：无 R2 层，`state_hash` 在 `run_ar25_r234.py` 804 行里 0 次 |
| 目标分解 | 有实现、不成产物：分解在 `Ls20Solver` 内部，`expected_goal` 是常量 `:318` | 有实现且是真分解：`solve_goal_decomp:117` / 双轴 `solve_goal_decomp_dual:276`；同样不进产物 |
| 路径 / 状态空间搜索 | 是：A* + 影子引擎 joint BFS `:175-227` | 是：`r3_beam_search:415`（300s / 3e6 节点 / width 8）+ `r3_state_search:365`（60s / 2e5） |
| 搜索预算有界 | 是：`{time_s, max_nodes, max_depth}` `:373-375` | 是：分层预算见头注 `:12-18` |
| 统一输出九字段 | 9/9 | **0/9** |
| plan 引用输入 state hash | 是（`input_state_hash = obs["state_hash"]`） | 否 |
| 候选动作属合法集合 | 事后校验并计入 verdict（`actions_legal`，`:450`） | 完全不校验；候选集写死 `[1,2,3,4,5]`（`:387/442/483/516`） |
| planner 只返回 abstract name，转换归 boundary | 形式违规：同一文件 `:448` 就转引擎枚举 | 潜在违规：六处 `ActionInput(..., data={})`（`:390/445/549/578/598/744`），而 `ACTION6.is_complex()==True` 且游戏侧处理它（`ar25.py:1792`） |
| 经 registry 按 `game_id` 选 planner | 否：`run_ls20_r2r3.py` 是 `__main__` 脚本，不在 `GameStrategyRegistry` | 否：线上 `agents/strategies/ar25.py:55-77` 只做罐头重放，零搜索 |
| 不伪造 WIN / 无私有绝对路径 | 是（`validity` 封顶 `verified_offline`） | 是（但只写 `result.json:799-801` + 手写 manifest 形状 dict `:768-776`，无 tick JSONL、无 recording） |

结论：**统一的 R3 还没做到——契约和算法各只在一半身上。** LS20 有契约没决策权（95/97 tick 走罐头 solver，A* 与 joint 0 次接管，见 7.2），AR25 有算法没契约（九字段 0/9）。按团队规范 `§7.3` 的"最小闭环通过"定义，两个游戏都没过：LS20 缺 registry 路由与目标分解产物化，AR25 缺 R2、九字段出口和逐 tick 证据。

另有一条重复实现要登记：`examples/run_ar25_r3_goaldecomp.py`（348 行，九字段同样 0/9）与 `run_ar25_r234.py` 的 `solve_goal_decomp` / `r3_state_search` / `r3_beam_search` 是函数级重复，连预算数值都一样，属 §7.4 "两份真源会漂移"的同类问题。

施工顺序（与 7.2 的"预算必须进内核"结论一致，不改动 AR25 的搜索算法本身）：
① 把九字段出口从 `run_ls20_r2r3.py` 提为公共件并加 `validate_plan` 闸门（现在 `evidence_compat.build_tick:67-79` 的信封里只有 `plan_id`，完整 plan 是 JSONL 行的兄弟键 `:471`，`validate_tick:110` 不校验 §3.2 那九个字段）；
② 让 AR25 的四层搜索套同一个出口（只加出口，不动 `solve_*` / 预算数值，避免回退 8/8 的既有成绩）；
③ 补 AR25 的 R2 与逐 tick 证据，即待办 ② 剩下的那一半。
反过来做等于给 AR25 单独再造一套协议，正是团队规范要避免的事。

## 8. 实施计划：统一 R3 plan 出口（①②）+ AR25 的 R2 与逐 tick 证据（③）—— 三步均已落地

对应 7.5 的三步顺序，**但标题里的"R3"只覆盖 ①②**：① 把九字段出口提为公共件、② 让 AR25 四层搜索套同一出口，这两步确实是 R3。③ 交付的是另外两层——**AR25 此前根本没有的 R2 观测层**（7.5 表第一行当时记"否：无 R2 层"）与 **§5 的逐 tick 证据层**（manifest / recording / report），R3 在 ③ 里只是换成消费 R2 算出的 `input_state_hash`。三步全部落地：① 见 8.7、② 见 8.8 / 8.9 / 8.10、③ 见 8.11；**8.1–8.6 是事前计划，与这三节实测冲突处以实测为准**。红线仍是 7.2 与 §1 的"不回退 LS20 7/7 / AR25 8/8"。

### 8.1 契约落点必须先定的那件事

新文件放 `lingjing_solo/planning/plan_contract.py`，不放 `arc_adaptor/`。理由：`pyproject.toml:28` 是 `packages = ["lingjing_solo"]`，editable 安装只暴露这一个包（`.pth` 内容就是仓库根），所以 ARC 侧线上策略能 import 的只有 `lingjing_solo.*`。`arc_adaptor` 在 ARC venv 里 `find_spec` 恰好返回 True，那是脚本自己把仓库根塞进 `sys.path` 的副作用，不能作为契约落点依据——放错位置的结果是"契约在离线跑测里能用、线上 import 不到"，正是要避免的第二份真源。

### 8.2 新增文件

| 文件 | 内容 |
|---|---|
| `lingjing_solo/planning/plan_contract.py` | `PLAN_SCHEMA = "lingjing-r3-plan-v1"`；`build_plan(...)`；`validate_plan(plan, *, legal_actions) -> dict`（fail-closed，抛 `PlanContractError`）；`PLANNER_REGISTRY: dict[str, str]`（planner 名 → 说明，注册式而非 if-else）；`subgoals` 为可选第 10 键 |
| `arc_adaptor/tests/test_plan_contract.py` | Tier A 纯单测，见 8.5 |

`validate_plan` 的六条断言：① 九字段齐全且类型正确；② `candidate_actions` 全部 ⊆ 传入的 `legal_actions`；③ `input_state_hash` 非空字符串；④ `search_budget` 三键（`time_ms`/`max_nodes`/`max_depth`）全为正整数，**键名统一到团队规范的 `time_ms`**（现有 `run_ls20_r2r3.py:373-375` 用 `time_s`，转换在 build_plan 里做一次，见 7.5 命名漂移条）；⑤ `planner` 必须在 `PLANNER_REGISTRY` 内；⑥ `validity ∈ {candidate, verified_offline, expired, none}` 且 `verified_offline` 必须带非空 `evidence_refs`。

### 8.3 改动点（①）

- `run_ls20_r2r3.py:313-331` 的手搓 dict 换成 `build_plan(...)`；`planner` 五个取值全部登记进 registry。
- `R3Planner.plan()` 内不再做名字→枚举转换：`:448` 的 `NUM_BY_NAME[...]` 挪到主循环（主循环本来就是 adapter 角色），出口只吐 abstract name。
- 目标分解产物化（7.5 第二条缺口的正解）：`expected_goal` 从常量改为 solver 现场（当前 leg / `goals` / `mod_tasks`），并填 `subgoals: [...]`。**只读不改判定逻辑**，`_solver_action` 的分支顺序一个字不动，以保证 planner 分布仍是 95/97。
- `evidence_compat.build_tick:67-79` 增加 `plan` 对象字段，`validate_tick:110` 内调用 `validate_plan`；`plan` 允许为 `None`（旧 recording 不带 plan，必须仍能通过 replay，对应 §7.2 第 5 条"existing recording 不被覆盖"）。

### 8.4 改动点（②：AR25 套同一出口）

只加包装、不动算法：`run_ar25_r234.py:620-660` 的层联回传已经是 `(path, method)` / `path`，`method` 串（`R0-已知解法`、`R3-目标分解`、`R3-双轴分解`、`R3-beam`、arc_shadow、greedy）逐项映射成 registry 里的 planner 名。四个出口 `solve_goal_decomp:117` / `solve_goal_decomp_dual:276` / `r3_beam_search:415` / `r3_state_search:365` 的函数体、预算数值、`_verify_and_return` 的校验语义全部保持原样——`validity` 由 `_verify_and_return` 是否通过决定。`expected_goal` 填 `perc["covered"]/total_targets` + 轴类型。

顺带修一处既有隐患（改完就是合规而不是扩权）：AR25 六处 `ActionInput(..., data={})` 走 `validate_plan` 后，复杂动作缺 payload 会被闸门拒并写 `fallback_used` 进 evidence（对应规则 5 与 7.5 表最后一行）。

### 8.5 验收（全绿才算 ①② 完成）

1. Tier A：`pytest arc_adaptor/tests/test_plan_contract.py` —— 九字段缺失各报各的错；`candidate_actions` 含非法动作 fail-closed；`plan=None` 的旧 tick 仍通过；`verified_offline` 无 `evidence_refs` 被拒。
2. Tier B：LS20 生产路径 `tools/ls20_live_strategy_check.py` 仍 `7/7 / 309 / WIN`；LS20 离线 G1 `verdict=PASS actions=309`；G2 仍 `2/7` 且 planner 分布与 7.2 逐项相同（这四项是"只加出口、没改决策"的探针）。
3. Tier B：AR25 重跑全 8 关，`won=True` 且总步数不劣于基线 278（v5，见 `e50c130`）。
4. 规则 7：所有测试在 ARC checkout 内用其 `.venv` 跑一遍（`71 passed / 3 failed` 基线不许变差），且在 Lingjing 根目录再跑一遍以证明 import 不依赖 checkout。

### 8.6 风险与 ③ 的预留

风险一：`build_tick` 加字段会让既有 report 的 `schema_ok` 判定变严，需连带核对 `replay_recording` 对缺 `plan` 的行的容忍。风险二：AR25 逐 tick 证据体量（beam 单关最多 3e6 节点展开），③ 时只记"决策 tick"、不记搜索展开，否则 recording 失去可重放性。风险三：planner 名从 `r3_ls20_solver` 改成 registry 名会让历史 `state/*/report.json` 的分布无法直接比对——保留旧名作为 registry key 的别名，不改写历史证据（§10 禁止证据替换）。

③ 的预留接口：`plan_contract` 只要求 `input_state_hash` 是字符串，AR25 当前没有 R2，先用现成的网格哈希占位（`r2_ls20.py:92 state_hash()`，通用版在 `r2r3r4_combo.py:28`）；等 ③ 落 `r2_ar25.py` 时换成正式 state hash，并把 `transition(before, action, after)` 补进 tick。**③ 已兑现，见 8.11**（交付 R2 观测层 + §5 逐 tick 证据层两处，不是一处）；只有一处与预留不同：占位用的是 ② 的 `sha256(repr(_state_key))`，不是 LS20 的网格哈希——理由见 8.8 偏差表第二行。

### 8.7 ①（含 R0）落地结果 —— 2026-09-19

本节是实测记录，与上面计划不一致的地方**以本节为准**；写本节（2026-09-19）时只有 ① 落地，② 与 ③ 仍是计划——② 见 8.8（同步清单与门禁：8.9 / 8.10），③ 见 8.11。

与 8.2 / 8.3 的三处偏差：

| 计划 | 实际 | 原因 |
|---|---|---|
| `PLANNER_REGISTRY` | 常量名 `PLANNER_NAMES` | 语义同上；改名的收益不值得留一份和代码不符的计划 |
| `arc_adaptor/tests/test_plan_contract.py` | `tests/test_plan_contract.py`（仓库根 `testpaths`） | 契约在 `lingjing_solo/`，单测跟着被测模块走；同时 `sync_to_arc.sh` 把它复制进 `tests/unit/`，两边都跑 |
| `expected_goal` 之外填 `subgoals` | `subgoals` 未填（契约里是可选第 10 键） | solver 现场没有"已分解的子目标序列"可如实填；宁可缺这个键，也不填一个拼出来的字符串 |

验收实测（四道全绿）：

1. Tier A：根目录 `pytest tests/` = **87 passed**（新增 17：16 通过 + 1 在无 `arc_adaptor` 的 checkout 里 skip）；`ruff` 对本轮 5 个改动文件全绿。
2. 规则 7：ARC checkout 内用其 `.venv` 跑 `tests/unit/test_plan_contract.py + test_lingjing_solo_agent.py` = **28 passed / 1 skipped**；全量 `tests/unit`（排除 `test_core.py`/`test_swarm.py` 的 `agents.structs` 缺失，属 §4.3 独立 dependency gate）= **89 passed / 1 failed / 1 skipped**。相对 71/3 基线：本轮 **修掉了原先两处 `solver._plan` 断言失败**（R0 生效），剩下的 1 个失败是 `test_action_recording` 需要未应用的 optional recording patch，与 ① 无关。
3. 生产路径：`tools/ls20_live_strategy_check.py --tag wip-plan-contract` = **`7/7 / 309 步 / WIN / verdict=PASS`**，逐关 12/45/39/43/44/72/53/1 步。
4. 离线 G1 `run_ls20_r2r3.py 400` = **`verdict=PASS actions=309`**；G2 `400 --no-scripts` = **`levels=2`、`actions=97`、planner 分布 `r3_ls20_solver 95 + r3_layout_wait 2`**，与 7.2 记录的分布逐项相同（决策逻辑确实没动）。`report.json` 的 `metrics` 新增 `plans_by_planner` / `plans_by_validity`，`criteria` 新增 `plan_hash_linked`。

顺带修掉的既有过度声明（不是本轮引入的）：原来 `plan()` 把**所有** `r3_ls20_solver` 动作标成 `validity=verified_offline`，但其中只有 `_script_mode` 下回放离线 BFS 罐头才算"离线验证过"，在线单步重规划和 `r3_layout_wait` 的等待步不算。现在只有罐头回放给 `verified_offline`，并且那种情况的 `evidence_refs` 额外带 `script_bank:ls20:L{n}` 好让人回溯；G2（关罐头）因此从"看起来验过"变成如实的 97 个 `candidate`。

一处契约设计上的取舍：planner 注册表（断言⑤）**只在生成时生效**。注册表由各运行器在 import 时填写，独立审计进程里没有它，若在读取路径也强制就使已落盘的 recording 再也读不动（违反 §7.2"不覆盖既有 recording"）。所以 `validate_plan(..., require_registered_planner=False)` 只用于 `_check_plan` 这条读取路径，①②③④⑥ 照查；测试里两向都锁住了。

R0 的实际口径：`LINGJING_LS20_PLAN` 显式注入 + `solver._plan` 作为"当前生效路线"镜像（`__init__` 就播种，坏帧/线上首帧解析不出网格时也有值可查；注入了就以注入序列为镜像，不会显示成"以为在用、其实没用"的罐头）。注入语义与 ARC 侧 `AR25Strategy.reset`（`agents/strategies/ar25.py:48-53` 的 `seeded_level = 99`）**对齐为整条序列**：一旦注入，本序列用完也不回罐头，交给在线规划。`_plan` 的刷新点在 `observe()`——罐头耗尽（`ls20_solver.py:1288`）与卡死回退（`:1256`）两处会清空它，所以只连调 `plan()` 而不喂新帧时镜像不会推进，这符合它"外部可观测的当前路线"定位，不是 bug。实测探针（真引擎 + `LINGJING_LS20_PLAN=ACTION4,ACTION2,ACTION2`）：`plan()` 前 3 步逐字吐 `ACTION4, ACTION2, ACTION2`，第 4 步起交回在线规划。这套 `_plan` 断言在 `arc_adaptor/tests/test_lingjing_solo_agent.py:59-74` 早已存在，本次是补齐实现，不是新增契约。

**§3.2 兼容要求第一条（planner 只吐 abstract name）的对账与收口**：包内原来唯一的转换点是 `lingjing_solo/agent.py` 的 `_try_game_action`（旧 `:378` 由 `_emit` 在 `cfg.return_game_action=True` 时调用）。它有两个问题：规划层知道了引擎枚举；更要紧的是返回类型取决于 `arcengine` 此刻能不能 import——装得上给枚举、装不上给字符串（本机 `.venv` 没有 arcengine，所以 `_emit("action4")` 实测返回的是字符串 `'ACTION4'`，这个坑是隐性的）。现在 `_emit` 恒返回 canonical 后的抽象名，转换收进边界 adapter `lingjing_solo/harness/kaggle_adapter.py:16 to_game_action()`（官方 Kaggle 入口要枚举，`MyAgent.choose_action` 按 `cfg.return_game_action` 决定转不转；`core/config.py:95` 的字段语义同时改成"只由 adapter 读取"）。闸门是 AST 级的：`tests/test_abstract_action_boundary.py` 扫 `lingjing_solo/**`，除 `harness/` 外任何模块 import `arcengine` 即失败（用 AST 而非 grep，因为注释里必须出现"arcengine"来解释为什么不能在那儿转换）。ARC 生产链路本来就不经过这里——`agents/templates/lingjing_solo_agent.py:100` 问的是 `strategy.choose_action`，枚举转换在它自己的 `:110 by_name` 里，`solo.choose_action` 全仓库无调用点，所以此改动对该链路惰性（实测 `7/7 / 309 / WIN`、G1 `PASS 309` 不变）。

**AR25 侧仍不合规，② 的工作量据此上修**：`run_ar25_r234.py` 的 planner 返回裸 int 序列，且在 planner 函数体内就转枚举——`ACT_MAP` 在 `:36`，转换点 `:390`（state_search）/ `:445`（beam）/ `:549`（greedy）/ `:578`（known solution）/ `:598`（`_verify_and_return`），`:663` 还把 `[ACT_MAP[n] for n in [1,2,3,4,5]]` 整个枚举列表交给 `arc_shadow`。所以 8.4 说的"只加包装、不动算法"不成立：② 需要把这五处转换搬到出口之外，并把出口从 int 改成 `ACTIONn` 名字（`validate_plan` 会拒 int，已有测试锁住）。函数体与预算数值仍可保持不变，但"零改动"这个预期要撤回。

落地时发现的仓库外风险已收口（2026-09-19 补）：`arc_adaptor/agents/strategies/__init__.py:4-8` import 的六个模块 `ar25.py / base.py / generic.py / ls20.py / r11l.py / registry.py` 曾**只存在于本机 ARC checkout 且整目录 untracked**。根因不是"从没纳管"，而是提交 `72b9069`（标题 `feat: 更新r234流程 - 添加新的策略文件和测试用例`）在加新文件的同时把这六个删掉（diffstat 共 −274 行），却没同步改 `strategies/__init__.py`——于是一边是我们仓库里 import 不通的包，一边是线上靠那六份 untracked 副本活着；`sync_to_arc.sh` 的 `cp -R` 只增不删，所以这个洞永远不会在同步时暴露。教训：**标题写"添加"的提交也要查删除**，`git show --stat` 里的负数行比文件列表更诚实。

拉回口径（不是简单 `git checkout 72b9069^ -- ...`）：五份（`ar25/base/generic/r11l/registry`）与删除前版本逐字节相同，说明 ARC 侧从未私改；**`ls20.py` 必须取 ARC 现版**——历史版 `from lingjing_solo.planning import LS20Solver` 并用 `set_plan()`，而当前包里只有 `ls20_solver.py:530 Ls20Solver` 与 `:655 reset_level()`，复活历史版等于往仓库放回一份引用死 API 的代码。实测：六份（合计 322 行）与 ARC 现存副本**字节相同**；在 Lingjing 侧 `import strategies` 现在能解析出全部 8 个符号，`LS20Strategy()` 实例化的 `solver` 类型是 `Ls20Solver`；无盘符路径、无 `ARC_API_KEY`、无 `arcengine` import，故 `tests/test_public_entry_paths.py` 与 AST 边界闸门均不红。门禁与拉回前逐项一致：根 `pytest tests/` = 97 passed、ruff 对六份全绿、ARC 内 targeted tests = **32 passed / 1 failed / 1 skipped**（失败项仍是需未应用 recording patch 的既有失败）、生产路径 = **`7/7 / 309 步 / WIN / verdict=PASS`**、G1 `PASS 309 hash_linked=True`、G2 `97 plans (95+2)`。

同一天顺手复检规则 1/2 时确认：两边 `agents/strategies/` 的**同名文件已全部逐字节一致**（21 个共有文件 0 漂移，之前"有差异"是我拿单边去 CRLF 比出来的假阳性），ARC 的 `agents/strategies/__init__.py` 与我们的相同（规则 2 保护的是上一层官方 `agents/__init__.py`，它确有 2 行手工注册未受触碰）。**本次按最小档执行，两项已知问题留待之后**：`sync_to_arc.sh:26` 的 `cp -R agents/strategies/.` 仍会把 `__pycache__/` 一起搬过去、且"同步产物 ⊆ 声明清单"不可机械核对；ARC 的 `agents/strategies/` 整目录 untracked（`tracked=0 / ondisk=27`），`cp -R` 覆盖同名文件时 `git status`/`git diff` 看不见被覆盖内容，规则 2 要求的"先看一下 diff"在这个目录里目前没有安全网。

### 8.8 ②（AR25 套同一出口）落地结果 —— 2026-09-20

改了 `arc_adaptor/agents/strategies/run_ar25_r234.py`（805 → 999 行）与新增 `tests/test_ar25_plan_contract.py`（17 项）。搜索算法、分支顺序、预算数值一律未动，所以 8.7 结尾那句"AR25 侧仍不合规"自本次起失效。

一处边界块收全部转换：`:44-79` 定义 `NAME_BY_NUM:52`、`ENUM_BY_NAME:53`、`AR25_ACTION_NAMES:56`（AR25 只用 1..5）、`ACTION_DELTA:58`、`to_enum:61`、`act_input:68`、`run_names:72`、`nums_to_names:77`。8.7 点名的五处 planner 内转换（`:390/:445/:549/:578/:598`）与交给 `arc_shadow` 的枚举列表（`:663`）现在只剩两类调用点：边界块本身，以及 `:833/:837` 仍把 int 表交给 `arc_shadow` 的老 API——那是它的 `_num2act/_act2num/_ACTS` 本来的键型，转换放在它**返回**的那一刻（`:841` → `nums_to_names`）。planner 出口：`_generate_actions:350`、`_dual_generate_actions:514`、`r3_state_search`、`r3_beam_search`（两者直接以 name 建树）、`R4Scorer`（stats 按 name 键、位移查 `ACTION_DELTA`）、`try_known_solution:718`、`_verify_and_return:742` 全部只吐 `list[str]`。

plan 出口：`r234_solve:760` 的返回改成 `(path, method, plan)`，`ar25_plan:209` 产九字段并当场过 `validate_plan`；四个新 planner 名在 `:91-98` 登记（`ar25_goal_decomp`、`ar25_goal_decomp_dual`、`ar25_shadow_state_search`、`r4_greedy_step`），复用已登记的 `t0_known_solution`/`beam`/`state_search`，`AR25_METHOD_TO_PLANNER:102` 留住历史 method 串以便与旧 `report.json` 对账（8.6 风险三）。`search_budget` 报的是代码真上限：`GOAL_DECOMB_COMBO_CAP:86` 与 `DUAL_COMBO_CAP:87` 从字面量提成常量，避免"plan 写一个数、代码跑另一个数"。

与 8.4 / 8.6 的四处偏差（以本节为准）：

| 计划 | 实际 | 原因 |
|---|---|---|
| 8.4 第二段：AR25 六处 `ActionInput(..., data={})` 之后由闸门拒复杂动作缺 payload 并写 `fallback_used` | 未做，且**不适用** | 实测 `GameAction.ACTION1..5.is_complex()` 全为 `False`（只有 ACTION6 为 True），AR25 根本不发带 payload 的动作；为不存在的分支写 `fallback_used` 等于凭空造证据 |
| 8.6：`input_state_hash` 先用现成网格哈希（`r2_ls20.py:92`） | 用 `ar25_state_hash:162`（sha256(`repr(_state_key)`)) 前 16 位 | `r2_ls20.state_hash` 只哈希 LS20 的 64×64 playfield，套到 21×21 的 AR25 上不含轴/拼块/选中/步数条，是"看着通用、实则漏信息"的假兼容。③ 落 `r2_ar25.py` 时替换 |
| 8.4：`expected_goal` 填 `covered/total_targets` + 轴类型 | 填了，但轴构成由 `ar25_axis_types:172` 自己从 `axes` 汇总 | `perc["atype"]` 按 R2 老约定只在**单轴**关卡填，双轴一律 `'?'`，直接用会让 plan 读成 `axes=2(?)`。② 不动 R2，所以只在 plan 层汇总（控制台 `[R2]` 行仍显示 `轴=2(?)`，这处不一致是故意的） |
| 8.4："只加包装、不动算法" | 算法与预算确实没动，但 4 个出口的返回类型从 `list[int]` 变 `list[str]` | 8.7 已预告此预期要撤回，本次落实：不改出口类型 `validate_plan` ②就拒 |

验收（本轮全部重跑，非引用旧记录）：

1. 行为零变化：改动前 `python arc_adaptor/agents/strategies/run_ar25_r234.py 8 0` = 8/8、`won=True`、**294 步**（`state/ar25_r234_20260920_075920_12168`）；改动后同命令 = 8/8、`won=True`、**294 步**，逐关 15/11/40/22/28/53/78/47 与基线逐项相同（`state/ar25_r234_20260920_081640_24104`）。注意 8.5 第 3 条写的"基线 278"本轮未能复核到，本轮实测基线是 294，红线按 294 算。
2. 出口合规：`result.json` 的 `plans_emitted=8`、`plan_schema=lingjing-r3-plan-v1`，planner 分布 `ar25_goal_decomp 3 / ar25_goal_decomp_dual 3 / t0_known_solution 2`；八份 plan 用 `validate_plan(require_registered_planner=False)`（审计口径）逐份重放通过，九字段 0 缺失，`candidate_actions` 全为字符串且 ⊆ `ACTION1..5`。
3. validity 只有两档 6/2：搜索产物 `candidate`，罐头解 `verified_offline` 且带 `arc_adaptor/arc_shadow.py:KNOWN_SOLUTIONS[6]` 这类可回溯 ref。
4. Tier A：`pytest tests/` = **114 passed**（① 时的 97 + 本次 17）；其中一条测试专②回退：拿旧的 `candidate_actions=[3, 4]` 走 `build_plan` 必须抛 `PlanContractError`。
5. lint：`ruff check` 对本文件 **6 F541**，与改动前同一份文件的 HEAD 副本逐条同数（F401/F841 由 `pyproject.toml:55-56` 全局 ignore，故 HEAD 那份里的 `step_str`/`li0` 不计），本次新增 0 项；新测试文件全绿。

② 之后 ③ 仍欠的两件事：AR25 一份 plan 覆盖**整关**（`levels[i]["plan"]`），不是逐 tick，所以 §7.3 的 `replay_complete` 依旧判不了——`result.json` 的 `limitations` 里已明写；以及 `input_state_hash` 的正式实现（见上表第二行）。另外提醒：已推上去、等提交的 `feature/r234-flow` 那份 PR **不含本节改动**（② 在推完之后才动），要进 PR 得再推一次。

### 8.9 ② 的新测试进 ARC 同步清单（规则 ①/⑦）—— 2026-09-20

三处声明改动 + 一处测试自身缺陷的修正，全部本轮实测：

1. `arc_adaptor/sync_to_arc.sh:37` 把 `tests/test_ar25_plan_contract.py` 复制成 `tests/unit/test_ar25_plan_contract.py`，并在 `:59` 写进 `synced_files` —— "同步产物 ⊆ 声明清单"对本次新增成立。`agents/strategies/` 的 `cp -R`（`:25`）仍是那处已知越界，本轮**没有**改复制方式（改法要你定）。
2. `arc_adaptor/MANIFEST.md` 原先只声明 `arc_adaptor/tests/`，而 `:31`/`:37` 这两份契约测试其实在仓库根 `tests/` 下——等于"同步了未声明的文件"。已补一条声明，写明二者及其在 ARC 的落点。
3. 同一份 MANIFEST 里"never copies `__pycache__`"这句是**假陈述**，本轮核实后改成如实描述：`arc_adaptor/agents/strategies/__pycache__/` 与 ARC 侧 `agents/strategies/__pycache__/` 两处都在（`ls -d` 命中），正是 `cp -R` 搬过去的。`.env`/`.venv`/recordings/API key 那半句仍成立，未改口。
4. 测试原来写死 `REPO_ROOT = parents[1]`：本仓库测试在 `tests/` 恰好对，ARC 在 `tests/unit/` 就指到 `tests/`，于是**明明存在**的运行器被报成"找不到文件"——skip 理由是假的。换成 `_runner_candidates()` 从 `__file__` 逐层向上枚举两种布局后，ARC 的 skip 落到真原因上。这坐实了 §7 的口径：**skip 也要 skip 在事实上**。

规则 ⑦ 实测（在 `F:\pro\ARC-AGI-3-Agents`，分支 `main`、HEAD `4743e7d`，用它自己的 `.venv/Scripts/python.exe`，`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1`）：`pytest -q -rs tests/unit/test_ar25_plan_contract.py tests/unit/test_plan_contract.py` = **16 passed / 18 skipped / 0 error**。18 个 skip 里 17 个是本次新增文件，理由统一为 `ModuleNotFoundError: No module named 'paths'`（运行器被 `cp -R` 带进了 `agents/strategies/`，但它 import `arc_adaptor/paths.py`，ARC 没那个目录）；第 18 个是 `test_plan_contract.py:125` 的既有 skip（`arc_adaptor/evidence_compat.py` 不在该 checkout）。**0 error** 是这次"带过去"唯一真正买到的性质：它证明这份测试进生产 checkout 不会把别人的 `tests/unit` 弄红；17 条断言本身仍只在本仓库执行。

本轮**没有**跑整脚本 `sync_to_arc.sh`：ARC 的 `agents/strategies/` 整目录 untracked（§8.8 末段登记过），`cp -R` 从我们这边盖过去时 `git diff` 看不见被覆盖内容——规则 ② 要求的"先看 diff"在那个目录里没有安全网；而且同步会一次推过去整个 `agents/strategies/`（源侧实测 27 个条目，含 `__pycache__/` 本身），本轮只需要落地一个测试。那里现存的 `run_ar25_r234.py` 仍是 ② 之前的副本（35622 字节，Sep 19 22:47），**将来跑同步会把它盖成 ② 版**——真要动那个目录，得先单独把 ARC 侧的 `git status` 核对一遍。本次只手工放了这一个测试文件，`git status --porcelain tests/` 显示它与另外四份同样只是 `??`（新增，未覆盖任何 ARC 原生文件）。

本仓库侧门禁：`pytest tests/` = **114 passed**（与 8.8 第 4 条同数，本次只换根路径推断、未增减用例）；`ruff check tests/test_ar25_plan_contract.py` 全绿；`bash -n arc_adaptor/sync_to_arc.sh` 通过。

一个待定判断：一份**在 ARC 里永远 skip** 的测试该不该长期留在复现包内。我的倾向是"留着但别高估"——它给 ARC 侧只买到"不炸别人"这一条，删掉 `:37`/`:59` 两行即可，② 的回归保护不受影响。

### 8.10 ② 门禁重跑 + 一次真跑 sync 的实际代价 —— 2026-09-20

门禁五道全部本轮重跑，不引用 8.8 / 8.9 的记录：

1. Tier A **按解释器分档**：系统 `python` 3.14.0（装有 `arc_agi`/`arcengine`）`pytest tests/` = **114 passed / 0 skipped**；`.venv` 3.12（无 `arc_agi`）同一命令 = **97 passed / 17 skipped**，17 条 skip 全部来自本次新增的 `tests/test_ar25_plan_contract.py`（`-rs` 逐条给出 `No module named 'arc_agi'`）。所以 8.8 第 4 条与 8.9 末段的"114 passed"只在带引擎包的解释器里成立——这句话本身没错，但**换了解释器就看不到那 17 条断言**，读的人必须按解释器算。
2. lint：`ruff check arc_adaptor/agents/strategies/run_ar25_r234.py` = **6 F541**；`git show HEAD:` 那份副本 `--isolated --select F541` 同数 6，本次新增 0。新测试文件 ruff 全绿。
3. AR25 行为：`python arc_adaptor/agents/strategies/run_ar25_r234.py`（默认 8 关）= **8/8、`state=GameState.WIN`、won=True、294 步**，逐关 15/11/40/22/28/53/78/47（`state/ar25_r234_20260920_083902_20412`）。八份 plan 逐份过 `validate_plan(require_registered_planner=False)`，九字段 0 缺失、字段集八关统一（14 键 = 九必需 + `schema_version/legal_actions/subgoals/ar25_method/level`）；`plans_emitted=8`、`plan_schema=lingjing-r3-plan-v1`、分布 `ar25_goal_decomp 3 / ar25_goal_decomp_dual 3 / t0_known_solution 2`、validity 6 candidate / 2 verified_offline；`search_budget` 与代码分档一致（L2–L4 `time_ms=60000`、L6/L8 `900000`、罐头解 `{max_nodes:1, max_depth:n}`）。
4. LS20 未被 ② 打回：生产路径 `arc_adaptor/tools/ls20_live_strategy_check.py --tag ar25-plan-outlet` = **7/7 / 309 步 / WIN / verdict=PASS**（逐关 12/45/39/43/44/72/53/1）；G1 `run_ls20_r2r3.py 400` = **verdict=PASS actions=309 hash_linked=True**（`plans={'r3_ls20_solver': 309}`、`verified_offline` 309）。
5. 规则 ⑦：ARC 内 `.venv` targeted（`test_plan_contract + test_ar25_plan_contract + test_lingjing_solo_agent`）= **28 passed / 18 skipped / 0 failed**；`tests/unit` 全量（排除 `test_core.py`/`test_swarm.py` 的 `agents.structs` 依赖 gate）= **89 passed / 1 failed / 18 skipped**，唯一失败仍是需未应用 recording patch 的 `test_action_recording`。与 8.7 的 `89 / 1 / 1` 相比只多了 17 条 skip，没有新的红。

**真跑了一次整脚本，并盖掉了一份未纳管副本**：`bash ../Lingjing-Solo-/arc_adaptor/sync_to_arc.sh .` 在 ARC checkout 内 exit 0、无 `cp` 错误，`synced_files` 现含 `tests/unit/test_ar25_plan_contract.py`。代价是它同时把 ARC 侧 untracked 的 `agents/strategies/run_ar25_r234.py`（8.9 记下 35622 字节 / Sep 19 22:47）盖成了 ② 版（48776 字节）。那份 35622 **在 git 里对不上任何版本**：该路径在 `--all` 历史里的三个 blob 是 20810 / 26886 / 33167 字节，HEAD 那份落盘（`core.autocrlf=true`，778 行）也只有 33945 字节——差约 1.7 KB 的 ARC 侧分歧没有第二份副本，`__pycache__` 里两份 `.pyc` 又都在覆盖后被重新生成，所以既无法比对也无法恢复。② 的证据不受影响（上面第 3 条就是在 ② 版上重跑并复现 294 步），但 8.9 那句"将来跑同步会把它盖成 ② 版，真要动那个目录得先核对 ARC 侧 git status"本次是真的发生了，只是我沿用了 8.9 之前的习惯先跑了脚本。

结论：#10 的 hash 闸门从"防将来"变成"补已发生"。具体要补的是——`cp -R agents/strategies/.` 之前先对 ARC 侧同名文件做 hash 比对，不一致即 fail-closed 列出差异清单（而不是静默覆盖），并把该目录纳入 `git status --porcelain` 的可见范围（或改成只复制声明清单里的文件、显式排除 `__pycache__`）。另：本次同步也顺带把工作树里那批"去私有路径"的散改（`run_ls20_r234.py`/`debug_ls20.py`/`probe_ls20_state.py`/`run_ls20_r2r3r4.py`）推进了 ARC——它们与 ② 无关、也仍未提交，属同一处"未提交内容照样进生产 checkout"的口子。

### 8.11 ③（AR25 的 R2 观测层 + §5 逐 tick 证据）落地结果 —— 2026-09-20

新增两份 stdlib-only 模块 + 一份 Tier A 测试，运行器接上它们；红线（LS20 7/7 / AR25 8/8）本轮逐项重跑，未回退。分层归属先说清，免得被"§8 = R3 计划"的标题带偏：`r2_ar25.py` 是 **R2**（§3.1 观测与状态身份，里面没有 planner 名、预算、目标选择任何一项），`tick_trail.py` 与 `manifest.json`/`recording.jsonl`/`report.json` 是**证据层**（§5），R3 在这一步只有一处变化——`input_state_hash` 改成消费 R2 算出的哈希。

| 文件 | 行数 | 职责 |
|---|---|---|
| `arc_adaptor/r2_ar25.py` | 253（新） | §3.1 观测 + 状态身份：`AR25_STATE_FIELDS:46`（引擎字段语义表）、`enrich:114`、`semantic_state:139`、`state_hash:170`、`engine_key_hash:175`、`observe:183`、`transition:233` |
| `arc_adaptor/tick_trail.py` | 306（新） | §5.2 写入器：`baseline:82` / `begin-`+`commit-`+`abort_attempt:102/108/119` / `record:127` / `attach_plan:150` / `check_continuity:172` / `flush_level:191` / `close:219`，审计 `_audit_hash:289` |
| `run_ar25_r234.py` | 999 → 1275 | 证据接线：`run_names:76` 收一个可选写入器参数、`_observe:109`、`_Attempt:119`、`_flush_level:157`、`ar25_state_hash:255` 转调正式哈希、`main:996` 建 trail 与三份产物、`1131-1149` 组判据与 verdict、`1204-1229` 写 manifest |
| `tests/test_ar25_recording.py` | 20 项（新） | 只依赖 stdlib，所以在**任何**解释器里都是真 assert；盯住"哈希算的是什么、回滚的行去哪、plan 盖在哪一行" |

**正式哈希换掉的是 ② 的占位**。② 用 `sha256(repr(_state_key))`，而 `_state_key`（`arc_shadow.py:175-190`）只收 `ayyvxqrhnzw` 的 (x, y, pixels) + 选中序号 + `hsiusrsrdkswnt` + `qehjebksqcm`；`_snapshot`（`:155-160`）save/restore 的却是这些**再加上** `ovoizfolxfq`/`ouurgkpbbjj` 的旋转距离映射、`hujpxmlafgh`/`xukxeewuexo`/`xjwpeqpcxav` 三个标志位和 `_state`。**已可判定的部分**：`_state_key` 里既没有 `_current_level_index` 也没有 `_state`（`arc_shadow.py:175-190` 逐行可查），而 `_snapshot` 会复原 `_state`、关卡切换也真发生过——把搜索键当状态身份，最早会在"两关布局恰好相同"时把不同关卡判成同一个，且完全看不见 `GAME_OVER`/`WIN` 这类终态。三个被掩的标志位（`hujpxmlafgh`/`xukxeewuexo`/`xjwpeqpcxav`）同理：`_snapshot` 认它们可变、搜索键不收。**待判定**：旋转距离那一栏的论证与 §3.5 的掩码依据（`:113` 说 `ovoizfolxfq` 是纯缓存、掩掉零信息损失）冲突，本轮的字段级归因还不足以定谁对（见 §3.8 末段与上面的计数器口径），所以这里不把它写成结论；正式哈希收着它的代价只是冗余，漏收的代价是把不同局面判成同一个，故按保守侧处理。现在状态身份 = `SEMANTIC_STATE_FIELDS:133`（schema/level/state/axes/pieces/sel_idx/total_targets/uncovered/rotation_distances/engine_flags）的规范序列化，`ENRICHED_KEYS:71` 三个字段只经 `enrich(g, perc)` 从引擎抄进来；`semantic_state:139` 缺任一项就抛 `R2PerceptionError`，**不算更粗的哈希**（测试第 2 条锁这条）。步数条照 `r2_ls20.py:92` 的 HUD 教训不进哈希，但每行写进 `game_specific.steps_left`，"这步只扣了一步、几何没动"因此仍可事后区分（`transition:233` 把 `moved` 与 `steps_delta` 分开发）。

**双向审计取代"我保证新哈希更好"**。搜索键 `engine_key_hash:175` 保留，只作对照：每个已提交动作比一次，两个方向分开计数（`_audit_hash:289`）。"语义变、搜索键没变"= 新哈希更细（预期）；反向 = 新哈希欠收，是缺陷。运行器据此把 `state_hash_not_coarser_than_search_key`（`1131-1149`）做成 blocking 判据。本轮实测：更细 **15** 次、欠收 **0** 次、只扣步 **0** 次。**但这三个数不区分段落**——`record:127` 无条件累加，`abort_attempt:119` 只丢行不回退计数，而对 294 行已提交转移复算 `before_engine_key != after_engine_key` 得 **0/294**，所以那 15 次全在被回滚的搜索展开里：它证的是"搜索过程中存在搜索键碰撞"，**不是**环境侧发生过什么（详见 §3.8，计数器按段落拆开是这条的后续修口）。判据本身不受影响：它看的是欠收方向，两个口径都是 0。

**回滚的搜索帧一行都不入证**（§10：「尝试过」≠「发生过」）。`beam`/状态搜索/`arc_shadow` 都在 `finally` 里 `_restore`（`arc_shadow.py:329-335`），所以那三层结束后引擎仍停在入口帧，唯一"真前进而不回滚"的动作是主循环的补放——它现在走 `_attempt()` 上下文并提交；被回滚的段落整段丢弃，本轮丢弃 **156 行**、提交 **294 行**，两个数都进报告。plan 是每关一份、在 `r234_solve` 晚于路径才造出来，所以已提交转移先进本关缓冲，`attach_plan` + `flush_level` 时才编号落盘：完整 plan 只盖本关第一行，其余 286 行带 `plan_id` 作 join 键（每行重复整关 plan 会让 recording 臃肿八倍）。`tick_trail` 与 `r2_ar25` 刻意做成 stdlib-only，Tier A 判据因此不跟着运行器一起 skip。

一处编号坑值得记（本轮测试逼出来的）：`baseline:82` 占 tick 0 且已把 `ticks_written` 记成 1，所以 `flush_level` 第一行必须是 `ticks_written + index`、`next_tick` 不能再 `+1`，否则整份 recording 凭空少一个 tick、`replay` 的连续编号检查就红。同理 `last_obs:247` 要能退回"已落盘的最后一行终态"（新增 `_flushed_obs`），不然每关 flush 之后它会退回基线，让第二关起每关都误报一次 `chain_break`。

验收（本轮全部重跑，不引用 8.8–8.10 记录）：

1. Tier A：`.venv`（3.12，无 `arc_agi`）`pytest tests/` = **117 passed / 18 skipped**；系统 `python`（3.14，有引擎）同一命令 = **135 passed / 0 skipped**。相对 8.10 的 97/114 净增 21 条 = 本次 20 条 + ② 那份测试新增 1 条（专盯 `input_state_hash` 是正式哈希、且 ≠ ② 的 `repr(state_key)` 占位）。
2. lint：`ruff check` 对两份新模块与新测试 **全绿**；`run_ar25_r234.py` **6 F541**，与 `git show HEAD:` 那份副本逐条同数，本次新增 0。
3. AR25 真跑（`python arc_adaptor/agents/strategies/run_ar25_r234.py`，`state/ar25_r234_20260920_093659_26020`）= **8/8、`won=True`、294 步**，逐关 15/11/40/22/28/53/78/47，与 8.10 第 3 条逐项相同 ⇒ ③ 只加证据层，未碰决策。产物：`recording.jsonl` 295 行 / 1.67 MB、`report.json` 七条判据全 true、`manifest.json` 发布 `state_hash_fields` + `state_hash_requires` + `engine_field_semantics`（含五个**语义未证实**的标志位，直说未证实）、`verdict=PASS`、`chain_breaks=0`、`plan_link=8/8`。
4. 证据由**协议层回读**判定，不信写入器自报：独立进程里 ticks `== range(295)` 连续、295 行逐行过 `validate_tick`（内嵌 plan 也过 §3.2 校验）、`replay_recording` 得 294 条转移 / `final_state=WIN` / `reset_count=0`；八份 plan 落在 tick `[1,16,27,67,89,117,170,248]`，各自 `candidate_actions` 长度 = 该关步数，合计 294 —— 与 `recorded_actions` 对得上。
5. LS20 未被 ③ 打回：`run_ls20_r2r3.py 400` = **`verdict=PASS actions=309 hash_linked=True`**（`state/ls20_r2r3_20260920_094008`）。附带一条踩过的坑：该运行器 `:392` 的 `max_ticks` **默认 200**，不带位置参数跑必然停在 200 步、`levels=5`、`verdict=FAIL`——那是调用方式错，不是回退（我第一次就误读了这个红）。LS20 运行器与 `r2_ls20` 本轮一字未改（`grep` 证实它不 import `tick_trail`/`r2_ar25`）。
6. 规则 ⑦：ARC checkout（`main`、`4743e7d`、它自己的 `.venv`）`pytest -rs tests/unit/test_ar25_recording.py test_ar25_plan_contract.py test_plan_contract.py` = **16 passed / 19 skipped / 0 error**。新测试那份给出的是**模块级** skip、理由如实：`被测模块不在当前 checkout 内: …\tests\arc_adaptor`。跑完把复制过去的单个文件删掉了——**没跑整脚本 sync**：`agents/strategies/` 在 ARC 侧仍整目录 untracked（本轮 `git status --short agents/strategies` = `?? agents/strategies/`），8.10 已经为一次 `cp -R` 盖掉一份无第二副本可恢复的文件付过代价。
7. 声明同步：`sync_to_arc.sh:42` + `:65` 与 `MANIFEST.md:20` 一起改，"同步产物 ⊆ 声明清单"对本次新增成立。§4.2 的边界照守：两份新模块**不在**白名单（引擎后端不得上线），产物只落 `state/`（未 ignore，提交走显式路径），ARC 原生 `agents/__init__.py` 的 2 行手工注册未受触碰。

与 8.6 预留的三处偏差（以本节为准）：

| 8.6 预留 | 实际 | 原因 |
|---|---|---|
| "③ 时只记决策 tick、不记搜索展开，否则 recording 失去可重放性" | 记**全部已提交动作**（294 行），搜索展开一行不记 | 判据应是"环境真发生过什么"，不是"哪几帧是决策"。体量因此可控（1.67 MB），而逐帧决策链的证据价值本来就在 plan 侧（见下条） |
| "`input_state_hash` 先用现成网格哈希占位，等 ③ 换正式哈希" | 换成 `SEMANTIC_STATE_FIELDS` 的 sha256[:16]，并**保留**搜索键作双向审计 | 见上文第二段。只换哈希不审哈希，等于用一次实测结论冒充永久性质 |
| 8.8 末段"③ 仍欠：AR25 一份 plan 覆盖整关、`replay_complete` 判不了" | `replay_complete` 本次**判了**（回读 294 条转移 = 提交数）；plan 仍是每关一份 | 逐 tick 证据补上的是"环境侧"，"逐帧决策链"属 plan 粒度，未冒充——`limitations` 里明写这份 plan 不能当逐帧决策证据 |

③ 之后仍未证的（报告里都写了，不改口）：引擎是 offline 的，够不着 Tier A 的 `live_scorecard`；`recording` 有帧但没有像素感知输入，`R1/R5` 无从复核；五个引擎标志位语义未证实（按 `_snapshot` 的可变性保守收进哈希）；`t0_known_solution` 在 `levels[i]["plan"]` 粒度下仍只是"整关一条"。③ 收尾后统一 R3 的三步（①②③）到此齐，剩下的独立待办是 #9（消除 `evidence_compat` 与分支 `protocol` 两份实现——本轮 `protocol_backend` 仍显示 `arc_adaptor.evidence_compat(fallback)`）与 #10（sync 的 hash 闸门 + `MANIFEST.md:7-8` 的过期 pin）。
